# Holdera — web corporativa

Empresa propia de Alex (no cliente externo). Holdera diseña e integra sistemas con IA para automatizar procesos y reducir costes en empresas. Sede: Barcelona. También hace webs, CRMs, SEO, Google/Meta Ads y ecommerce, pero la esencia (y el foco de la web) es la automatización con IA: se sientan con el cliente, escuchan qué quiere automatizar, explican cómo lo harán y lo integran en sus sistemas o lo construyen de cero.

## Estado
- Se construye por pasos. **Paso 1 (hecho, 07-09-2026): hero landing** replicando 1:1 la referencia "Zenrixa" que pasó Alex (hero gris claro de estudio, grotesk, acento naranja, figura central, widget derecho, partners, barra inferior) + transición de scroll hacia una "hoja" con los primeros bloques de contenido (qué hacemos, cómo trabajamos, contacto).
- **Paso 2 (hecho, 07-09-2026): holograma de cerebro + hoja clara.** Seis cambios pedidos por Alex:
  1. El drawer lateral era translúcido (`--panel` al 94 %) y se leía el hero a través. Ahora es opaco.
  2. El robot se sustituye por un **holograma de cerebro**: nube de 10.000 puntos en WebGL (`brain.js`), con el mismo tipo de flotación/giro por scroll que el GT3 RS de serresdrive.com (referencia que pasó Alex; el código real está en `Desktop/serres-drive-main/js/experience.js`).
  3. La hoja de contenido pasa de casi negra a **casi blanca**.
  4. Al bajar, el contenido del hero **sube** y el cerebro **baja**: dos capas que se separan y dejan ver el wordmark y la hoja que entra.
  5. Los partners llevan **sus logotipos reales** (simple-icons, CC0), no rombos neutros.
  6. La barra inferior lleva cuatro chips repartidos por igual — Diseño de webs · Estrategia digital · Marketing · Automatización con IA — y **sin puntos naranjas**.
- **Paso 3 (hecho, 08-09-2026): hoja INVERTIDA a oscuro + peces + planeta.** Cuatro encargos de Alex:
  1. **La hoja entera pasa a oscuro** (decisión suya: hero gris claro, y de la hoja al footer todo oscuro, una sola transición). Los nombres de los tokens `--sh-*` no cambian: la inversión es un cambio de paleta, no una reescritura, porque cada regla ya leía el color de la hoja de un token.
  2. **Holograma de peces** (`fish.js`) en "Qué hacemos": seis peces girando alrededor de un embudo de ocho aros. Los aros del modelo original se sustituyen por **texto** — cada aro lleva una frase que gira con él.
  3. **Colores del cerebro por curvatura real** (`brain.js` + `_build/brain-shade.js`): la nube ya no es tinta plana con confeti naranja, sino que se sombrea por dónde está el pliegue.
  4. **Planeta que gira con el scroll** (`planet.js`) en "Cómo trabajamos", recreando el componente "Scroll-Spinning Earth" que pasó Alex (referencia Next + react-three-fiber + framer-motion-3d + Lenis) en WebGL a mano, sin ninguna de esas dependencias. Sustituye a la rejilla de cuatro tarjetas iguales que había ahí (patrón prohibido por el CLAUDE.md global).
- **Paso 4 (hecho, 08-09-2026): tejido Perlin + cerebro reactivo + escaparate de servicios.** Tres encargos de Alex, cada uno con su referencia en vídeo en la raíz del proyecto:
  1. **Tejido de líneas Perlin** (`waves.js`, ref. `background-animations-5.mp4`) de fondo del hero. Discreto, por DEBAJO de todo el texto, reactivo al ratón.
  2. **El cerebro reacciona al ratón como la gota de agua** de la referencia VWLAB (`3d-animation-6.mp4`): el puntero abre un canal en la nube y los puntos vuelven fluyendo.
  3. **Escaparate de servicios** (`grid-animations-8.mp4`, "Directional Hover Reveal"): los cinco servicios en una lista abajo a la izquierda del hero; al pasar el ratón por un nombre el hero entero se oscurece, entran tres paneles de obra con cuñas de `clip-path` desde distintos bordes y aparece el título grande del servicio.
- **Paso 5 (hecho, 08-09-2026): sitio completo — bugs, páginas interiores y SEO.** Encargo de Alex en una sola tanda:
  1. **Bugs.** Revisión multiagente (72 hallazgos, 14 confirmados por dos escépticos, el resto refutado o sin verificar por el límite de sesión) + pruebas en Chrome. Arreglado: los cinco enlaces de «Servicios» llevaban todos a `#servicios` (= diapositiva 0, «Estrategia digital» aterrizaba en Asesorías) → cada diapositiva del deck tiene `id="servicio-<x>"` y `script.js` desplaza el deck hasta ella; «Servicios» del drawer iba a `#como-trabajamos`; «Qué hacemos» iba al proceso y ahora va a `#servicios`; los títulos de paso se rastreaban como «EscuchamosEscuchamos» (dos spans) → un solo texto + `::before` con `attr(data-word)`; los cinco `<h3>` del escaparate rompían el esquema de encabezados → `<p>`; el H1 entraba INVISIBLE (ver trampas); el título del deck se recortaba en móvil; la hoja móvil dejaba dos «orejas» oscuras en sus esquinas; el aside del hero no acompañaba a la columna en alturas cortas; foco perdido al ocultarse la isla; hover de pasos en móvil pintaba tinta sobre casi negro; 190 líneas de CSS y 8 tokens de secciones retiradas (peces, features, terminal, scrim) fuera; `.gitignore` con las fuentes 3D y los vídeos de referencia.
  2. **El bug de «la nav desaparece al volver arriba» (R1), reproducido y arreglado de raíz.** No salía con scroll programático, Fin/Inicio, clic en la isla, drawer abierto abajo + «Inicio» ni recarga (solo un tramo de 1,2–1,8 s de scrub rezagado). Sí sale con **cualquier `ScrollTrigger.refresh()` con la página desplazada** (resize, maximizar, DevTools, zoom, `load` tardío): con `html { scroll-behavior: smooth }`, el `scrollTo(0)` que ScrollTrigger hace para medir se convierte en un deslizamiento suave, su caché ya cree que está a 0 y el escenario se mide en la posición real → **`start = −scrollY`** (medido: `start −7975 / end −6625`), toda la página es progreso 1, la nav queda a `opacity: 0`, copy y aside a 0,22, y el deslizamiento pendiente lleva la página arriba sola. Arreglo en `initScroll`: `refreshInit` fuerza `scroll-behavior: auto` inline con un flush de estilos (y `refresh` lo retira un tick después), `start`/`end` numéricos (0 y alto del escenario − viewport) que ni siquiera pasan por la medición por rect, `clearScrollMemory()`, `is-scrolling` nunca con la página arriba, y **auto-curación** en cada scroll a `≤ 2px` si el disparador no está en reposo. Verificado: refresh a 7975 → `0 / 1350`, y de vuelta arriba nav a opacidad 1. Además `onLeaveBack` remata el scrub al instante y `.is-settled` apaga las transiciones de `.nav`/`.hero__wordmark` cuando la entrada ha terminado.
  3. **Escaparate con fotos reales** (Pexels, licencia libre, elegidas a ojo entre ~110 candidatas; créditos en `assets/img/servicios/CREDITS.json`). Sustituyen al arte generado — la «línea que sube» de Estrategia digital era `estrategia-a.webp` y ya no existe. **Las 20 fotos y el generador se borraron en el paso 10**, cuando el escaparate pasó a enseñar capturas del propio panel.
  4. **Nav de siete entradas** (Inicio · Servicios ▾ · Cómo trabajamos · Nosotros · Partners · Panel demo · Contacto), misma en las siete páginas y guardada por `_build/check-shell.js`; el drawer tiene siete filas y `overflow-y: auto`.
  5. **Páginas nuevas**: `nosotros.html` (sectores: inmobiliarias, hoteles y restauración, B2B, B2C — y que cualquier proceso vale), `partners.html`, `contacto.html`, `aviso-legal.html`, `privacidad.html`, `404.html`; comparten `pages.css` y una banda clara `.phead` + hoja oscura. **Todo dato de contacto es un marcador** `[… por confirmar]` (`data-placeholder`), por petición de Alex.
  6. **SEO técnico**: `_build/seo-inject.js` (canonical, OG/Twitter, iconos, manifest, JSON-LD por página desde `_build/seo/meta.json`), `sitemap.xml`, `robots.txt`, `site.webmanifest`, `.nojekyll`, `assets/seo/og-*.png`, `assets/icons/*`. Dominio en UNA constante (`SITE`); en el paso 7 pasó a ser `https://holdera.es`.
  7. **Geist alojada en el sitio** (`assets/fonts/`, 82 KB en cuatro subconjuntos; `_build/fetch-fonts.js`) en vez de la hoja de Google Fonts, que bloqueaba el H1. **GSAP se inyecta desde `script.js` solo en escritorio** sin movimiento reducido: el móvil ya no baja 42 KB que no usaba. Las texturas del planeta (410 KB) se piden cuando la sección se acerca.
- **Paso 6 (hecho, 08-09-2026): la barra de navegación acompaña al scroll.** Encargo de Alex («la barra horizontal ha de seguir bajando mientras scroleas»). La cabecera sale del hero y de la banda de las páginas interiores y va **fija** al principio del `<body>` en las siete páginas (misma copia del shell). Tres estados que escribe `script.js` por posición de scroll: `is-scrolled` (>8px: cristal claro `--nav-glass-light` y barra compacta de 64px), `is-over-sheet` (el borde superior de `main` ha llegado a la barra: cristal oscuro `--nav-glass` y paleta on-ink) e `is-veiled` (escaparate: paleta on-ink sin cristal). GSAP ya no toca la nav; **la isla flotante (`.fab`) ha desaparecido** (markup, CSS, JS y tokens `--fab-bg`/`--z-fab`); el centinela del hero sigue existiendo solo para dejar el hero `inert` cuando la hoja lo tapa. `contrast.js` vigila el texto y el botón MENÚ sobre los dos cristales.
- **Paso 7 (hecho, 09-09-2026): publicado.** La web está EN VIVO en **https://holdera.es**, servida por Hostinger desde el repositorio de GitHub. Ver la sección «Publicación» más abajo: el dominio, el repo, cómo despliega y las cuatro trampas que costaron la sesión.
- **Paso 8 (hecho, 10-09-2026): la web funciona de verdad en el móvil.** Encargo de Alex: «en el móvil está fatal hecha, muy bugeada, nada de las cosas que se tienen que mover se mueven, está completamente estática». Auditoría de 12 agentes + 12 escépticos (121 hallazgos, 119 en pie, 45 extras) más verificación propia en Chrome emulando iPhone 390x844 y Android 360x640.
  1. 🔴 **El cerebro estaba congelado, y era exactamente la queja.** `readProgress()` en `brain.js` empezaba con `if (!stage || !DESKTOP.matches) return 0`: en móvil el progreso valía **0 siempre**, y de él cuelgan el descenso, el giro (1,6 vueltas) y el escalado. Los tres anulados; solo quedaba el balanceo de reposo. Quitada la puerta — la fórmula ya servía en móvil, porque por debajo de 901px el hero no está pineado y `.hero-stage` mide su alto real (1470px, rango 626px). Las amplitudes se recortan en móvil (descenso 1,55 → 0,62 unidades de mundo, giro ×0,45): la caja es menos de la mitad de alta y `.hero` tiene `overflow: hidden`, así que 1,55 (~217px) habría sacado del cuadro una nube de 219px. Medido tras el cambio: el centroide baja 30,4px entre progreso 0,10 y 0,48.
  2. 🔴 **El planeta se dibujaba encima del texto.** `.orbit__sky` es `position: absolute; inset: 0` con el canvas centrado y `.orbit__steps` encima. En escritorio funciona (títulos de 108px, planeta de 620px de telón); en móvil aterrizaba justo sobre los pasos 02 y 03. Medido con `_build/png-probe.js`: «Diseñamos» **1,12:1** y su descripción **1,00:1**; «Integramos» 1,21:1 y 1,00:1. En móvil el cielo pasa a ser un bloque normal por encima de la lista → las 8 regiones pasan (4,51:1 títulos, 6,35:1 descripciones).
  3. **El deck de servicios dejaba ~600px de negro por ficha.** La obra que llena ese hueco en escritorio es el escaparate de PUNTERO, así que en móvil las 20 fotos no se pedían nunca (0 de 20, verificado). Cada ficha lleva ahora su panel `-b` en diferido (`.deck__art`, solo ≤900px). Contenido por ficha 297px → 538px; coste en carga inicial **cero** (145 KB al llegar).
  4. **Movimiento reducido + móvil: 1.735px de vacío.** El bloque `prefers-reduced-motion` pone el escenario del deck en `height: auto` y el bloque móvil, POSTERIOR y con la misma especificidad, lo devuelve a `420dvh`. Arreglado con una consulta combinada al final del archivo (ver trampas).
  5. **iOS hacía zoom al tocar cualquier campo**: estaban a 15px y Safari amplía por debajo de 16px sin volver solo.
  6. **Cada ancla aterrizaba debajo de la barra**: no existía `scroll-padding-top` y la cabecera es fija desde el paso 6 → 64px de cada sección ocultos, en las 7 páginas y también en escritorio.
  7. **Anillo de foco invisible en los pasos** (`--ink` sobre la hoja: **1,01:1**) porque en móvil no se abre la banda naranja sobre la que ese aro está pensado.
  8. **Ergonomía táctil**: MENÚ 40px, cerrar del drawer 40px, pie 22px, lista del hero 34px, pills 39px, índice legal 38px → todos a 44px por padding/min-height, sin tocar ni un tamaño de letra.
  9. **Batería**: techo de 60 fps al tejido Perlin solo en móvil (medido 144 → 48 repintados/s sobre un buffer de 1,32 Mpx).
  10. **SEO 92 → 100**: los 37 marcadores de contacto eran `<a>` SIN href desde el paso 7 (`crawlable-anchors`). Convertidos a `<span>`.
  11. 🔴 **Sellado de assets con hash de contenido** (`_build/version-assets.js`), encontrado verificando el despliegue en vivo. Ver la sección «Publicación».
  - **Resultado**: Lighthouse móvil en **https://holdera.es 100/100/100/100** con 0 fallos; contacto y nosotros igual. Las 4 puertas pasan. Las 7 páginas a 390x844 y 360x640: sin scroll horizontal, 0 anclas sin href, 0 inputs bajo 16px.
- **Paso 9 (hecho, 19-09-2026): el panel demo pasa a ser el PRODUCTO real.** Alex subió `holdera-product-hotel-operations-v1/` —una app de Next.js 16 / React 19 / PGlite, 291 archivos, su propio repo— y pidió sustituir el panel demo de empresa genérica por ella. Y decidió el **pivote completo de la web a software hotelero** (ver la memoria del proyecto).
  1. **No era un copiar y pegar, y no podía serlo.** Las páginas son Server Components que consultan PGlite en cada petición; `next.config.ts` no tenía exportación estática y las rutas `/api` no son exportables. En holdera.es no hay runtime de Node. Pero el producto es **determinista y está congelado** el jueves 15 de enero de 2026 a las 14:30 (Europe/Madrid): emite siempre el mismo HTML, así que se **captura** una vez y se sirve como archivo. Eso es `_build/snapshot-panel.js`.
  2. **259 páginas** (100 habitaciones, 57 métricas, 60 días de histórico, 5 plantas, 6 zonas, 4 casos), 54 MB en crudo y **7,4 MB comprimidos**; unos 45 KB por página para quien la visita. 132 respuestas de API precalculadas a JSON.
  3. **Lo que costó que funcionara de verdad** (cuatro fallos que una captura de pantalla habría dado por buenos, todos en «Trampas del paso 9»): las cinco fuentes daban 404 en silencio, React no hidrataba, el `basePath` rompía la llamada que la app se hace a sí misma, y el router de Next navegaba a rutas sin barra final que no existen como archivo.
  4. **El panel viejo se elimina entero**: `panel.html`, `panel.css`, `panel.js`, `panel-data.js`, su tarjeta social y todas sus referencias en el tooling, la memoria y esta ficha. Recuperable con `git show 93e3d35:panel.html`. Motivo de fondo, además del pivote: enseñaba cifras inventadas (27.560,64 €, «+14,1 % vs mes anterior») y la tesis entera del producto nuevo es que **ninguna cifra se enseña sin poder trazarla hasta su registro de origen**.
  5. **Dos párrafos legales eran falsos** desde el cambio y se reescribieron, no se borraron: `privacidad.html` §8 y `aviso-legal.html` §4 decían que el panel guardaba su estado en `localStorage`. Eso era el panel viejo; el nuevo **no guarda nada** (verificado: 0 archivos de `panel/` usan `localStorage`, `sessionStorage` ni cookies).
  6. **`Redirect 301 /panel.html /panel/`** en el `.htaccess`: esa URL estuvo indexada y en el sitemap diez días.

- **Paso 10 (hecho, 19-09-2026): la web ES el producto, y el panel se rediseña entero.** Tres encargos de Alex en una tanda: más Revenue, la web girada al producto, y el panel con la estética del hero. Construido con 8 agentes en paralelo + 8 escépticos (ficheros repartidos sin solape) y rematado a mano.
  1. **Revenue deja de ser una tarjeta y pasa a ser un panel de mando.** Fila de tres periodos (Tonight · Month to date · Next 30 nights), cada cifra con su comparación contra el mismo momento del año pasado; bloque de TRevPAR con el reparto por departamentos; gráfico de 61 noches (30 cerradas + 31 on the books) con la misma noche del año pasado superpuesta y un conmutador Total revenue / RevPAR / ADR; curva de pace; y las tablas de mezcla por tipo de habitación y por canal. Todo SVG a mano, navegable con teclado: no hay librería de gráficos y no se ha añadido ninguna.
  2. **Reservations** (`/revenue/bookings`, entrada nueva en la barra lateral bajo Commercial): calendario mes a mes de diciembre 2025 a julio 2026 con ocupación, ADR, habitaciones on the books, pickup de 7 días y pace contra STLY por día, más el desglose de la noche seleccionada por tipo de habitación, canal y departamento. **El cambio de mes es estado del cliente, nunca `?month=`**: el capturador estático tira las rutas con query que no estén en su lista blanca.
  3. 🔴 **ADR y RevPAR ya no son constantes escritas a mano.** `src/demo/hotelOperations/bookings.ts` es un libro de reservas determinista (curva de pace por día de la semana, tarifa por tipo, cuatro departamentos declarados) que **reconcilia con el export diario de 60 días noche a noche**, y hay un test que lo comprueba. La portada decía 56 habitaciones donde el dataset dice 55, ADR 180 € y RevPAR 109,57 €; ahora salen 55/92, ADR 162,41 €, RevPAR 97,09 € y TRevPAR 164,03 €.
  4. **Las fórmulas se van de la portada.** `revenueEvidence.ts` + `MetricDetail.tsx`: el KPI enseña la cifra y su comparación, y «Detail» abre tres pasos —qué mide · fórmula con el valor que tomó cada entrada · fuente y supuestos declarados—. La fórmula **se lee del catálogo de métricas**, así que hay una sola definición por métrica en todo el producto. No pasa por `/api/explain`: ese endpoint solo responde por las dos métricas de ocupación que Core persiste con linaje.
  5. **Nomenclatura de hotel**: Ready → **Vacant Clean (VC)**, Dirty → **Vacant Dirty (VD)**, Being cleaned → **Cleaning (IP)**, In house → **Occupied** en el eje de estado de habitación (se conserva «In house» donde el sujeto es la ESTANCIA), Out of Order **(OOO)**, Out of Service **(OOS)**, «Still to arrive» → **Due in**, «Still in house» → **Due out**. Solo etiquetas: los ids del enum siguen intactos porque generan las clases CSS y viajan en el payload de `/api/today`.
  6. **Visual System v4 «Studio Light»**: el panel pasa del negro cálido a la estética del hero —gris de estudio, Geist, tinta, un solo naranja— con la paleta de estado re-derivada para fondo claro y partida en valor de TEXTO y valor DECORATIVO, igual que hizo la web con `--accent-ink` / `--accent-hero`. **La capa Evidence y el modelo 3D del hotel siguen oscuros a propósito**: es la misma idea que el grupo `--on-ink-*` de la web (superficies oscuras sobre página clara), y el edificio se lee como una maqueta sobre una mesa iluminada.
  7. **La web gira entera al producto**: fuera los cinco servicios de agencia, el submenú «Servicios», los ocho logotipos de tecnología (afirmaban integraciones que no existen, y dos de ellos IA que el producto no tiene), las estrellas decorativas y «+30 empresas automatizadas». El hero es «Todo tu hotel. Cada cifra, con su origen.», la lista del hero son las cinco pantallas del producto, y el hueco de los logos lo ocupa la cadena de evidencia (01 Fórmula · 02 Hechos · 03 Registro · 04 Dato en bruto). `partners.html` deja de ser un muro de logos y pasa a ser «cómo se conecta»: cuatro fuentes, qué enciende cada una y **qué no se puede enseñar sin ella**.
  8. **El escaparate del hero enseña el producto, no fotos de banco.** `assets/img/producto/*.webp` (20 archivos, 805 KB, los mismos tamaños que la obra anterior) son capturas del panel ya rediseñado, recortadas por `_build/producto-art.js` desde cinco capturas de página entera.
  - **Resultado**: `tsc` limpio, 384 de 385 tests en verde (el que falla es de entorno, ver trampas), las cinco puertas pasan, Lighthouse móvil 100/100/100 en la home y en `/panel/`, sin scroll horizontal a 390px y con el orden de encabezados correcto.
- **Paso 11 (hecho, 20-09-2026): el panel se reconstruye sobre las Human Interface Guidelines de Apple.** Encargo de Alex en una sola noche: «reinventa entera la sección del panel demo, centrada por completo en el diseño de Apple; el calendario, inspírate en la app Calendario». No es "inspirado en": la escalera tipográfica, el sistema de color, los materiales, la geometría de los controles y las curvas de movimiento son de Apple, leídas de la HIG entera (172 páginas descargadas de su API de documentación, porque la web es JS y WebFetch no la ve) y, donde la HIG da un número, se usa ese número. Doctrina del producto intacta: el color sigue siendo estado y nunca dirección, la traza sigue acabando en el registro RAW, y lo que falta sigue faltando.
  1. **La idea que reorganiza la pantalla: dos capas.** Una capa FUNCIONAL translúcida —barra lateral, barra superior, hojas, controles flotantes— que flota sobre una capa de CONTENIDO opaca que nunca es cristal. Antes todo pesaba lo mismo: un rectángulo blanco con un filete de 1px. Es literal de la HIG («Don't use Liquid Glass in the content layer»).
  2. **Tipografía**: la escalera Dynamic Type de iOS/iPadOS «Large (default)» — Large Title 34/41 … Caption 2 11/13 — con la tabla de tracking de Apple pasada a em. El cuerpo sube de 14 a 15px y la jerarquía por fin existe. **Fuera las versales**: 52 reglas ponían micro-etiquetas en mayúsculas con tracking; Apple no lo hace casi en ningún sitio y a 11px se leen peor que un Footnote normal.
  3. **Color**: la paleta del sistema (systemOrange/Green/Red/Brown/Gray) con sus jerarquías label / fill / separator / grouped background. El acento sigue siendo el naranja de Holdera — la HIG lo bendice explícitamente— y systemOrange #FF9500 resulta ser casi gemelo del #f08a24 del sitio. Un relleno de acento lleva TINTA encima (7,74:1), nunca blanco (2,20:1).
  4. **Forma y elevación**: radios concéntricos, cápsulas para los controles, `corner-shape: squircle` donde el navegador lo tiene (Chrome 139+), y **el contenido agrupado NO proyecta sombra**: la profundidad es el escalón de fondo y el separador. La sombra queda para lo que flota de verdad (el pulgar del control segmentado, los popovers, las hojas).
  5. **Movimiento**: los muelles de SwiftUI escritos como `linear()` de CSS, generados de la ecuación del muelle (`.snappy` respuesta 0,4 amortiguación 0,85; `.smooth` respuesta 0,5 crítico), no béziers a ojo.
  6. **Modo oscuro completo**, desde el primer día. La rampa oscura de Apple desplazada un escalón para no usar #000000 (prohibido por el CLAUDE.md global): lienzo #1C1C1E, tarjetas #2C2C2E, elevado #3A3A3C.
  7. **El calendario de Reservas es Calendario de Apple**: el mes es un Title 1 con un par de chevrons en cápsula, los meses son un control segmentado, la rejilla es UNA superficie continua sin canaletas, y **el estado vive en el NÚMERO del día** — hoy es un círculo relleno de acento. Lo que no tiene Calendario y se queda porque es el producto: cada noche con su ocupación, su tarifa y su pickup.
  8. **Evidence deja de ser una isla oscura** y pasa a ser una hoja de inspector flotante en el material del sistema: la voz MONO y la cadena de linaje son lo que dice «esto es técnico», no la oscuridad. La HIG llama a lo otro «an app within the app». **La escena del hotel sí se queda oscura**: sus 42 materiales y su luz interior cálida SON la señal de «in house».
  9. **Iconos redibujados con la geometría de SF Symbols** (retícula de 16, trazo 1,6, misma caja óptica de 13 para todos) y dos glifos nuevos que dicen mejor lo que son: Reservas lleva la rejilla del mes entera, History pasa de un segundo gráfico de barras a `clock.arrow.circlepath`.
  - **Resultado medido**: 0 fallos de contraste en 18 pantallas × claro y oscuro × 1440 y 390 (3.032 elementos de texto por pasada en escritorio, 5.572 en móvil), 0 scroll horizontal a 390/1024/1180/1366/1440/1920, Lighthouse móvil en `/panel/` accesibilidad 100 · buenas prácticas 100 · SEO 100, 384 de 385 pruebas (la que falla es la de entorno de siempre).

- **Paso 12 (hecho, 20-09-2026): el panel vuelve a navegar sin recargar, y la cámara entra en la habitación.** Alex comparó la demo servida por Next (holdera.vercel.app) con la nuestra: «cuando entras a una habitación, en el suyo es mucho más smooth». Tenía razón, y eran **dos fallos distintos**, los dos de raíz y los dos anteriores a esta sesión.
  1. 🔴 **La navegación era DURA.** El router del App Router no navega leyendo el HTML: pide a la MISMA URL un flight payload, con la cabecera `RSC: 1`. El host estático le devolvía el documento HTML —355 KB donde Vercel manda 340 bytes de payload—, el router veía que no es lo que pidió y se rendía recargando la página entera. Consecuencia visible: la escena 3D, que vive en el layout de `/rooms` y **no debería desmontarse**, se desmontaba y se volvía a montar, así que el movimiento de cámara de 760 ms no ocurría nunca. Arreglo: `snapshot-panel.js` guarda ahora el payload de cada página como `index.rsc` junto a su `index.html` (260 payloads, 9,9 MB) y el `.htaccess` lo sirve con `RewriteCond %{HTTP:RSC} =1`.
  2. 🔴 **Y aun recargando, la cámara nunca entraba.** `viewFromPath` compara con `/^\/rooms\/([^/]+)$/`, y el despliegue compila con `trailingSlash`, así que `usePathname()` devuelve `/rooms/502/` **con barra final**: no casa, y la vista caía en `{kind:"building"}`. Lo mismo en las zonas de Ground Floor (`/operations/restaurant/`). Roto desde el paso 9; en `next dev`, que no lleva barra final, no se ve. Arreglo: `routePath()` en `spatialNavigation.ts`, normalizando en un solo sitio.
  3. **De propina, un fallo de datos que ya estaba**: el conmutador de fecha te llevaba a `?date=2026-01-14` y te enseñaba el **15** de enero. El `<Link>` guarda su href en las props —el flight data—, no en el atributo del DOM, así que reescribir solo el atributo no cambiaba a dónde navegaba el router. Ahora se reescribe también el flight data, y los enlaces con query (cuya carpeta no es una ruta que la app conozca) se le quitan al router con un interceptor de clic: navegación normal del navegador al archivo correcto.
  - **Medido** con `node _build/nav-check.js`, que pone una marca en `window`, pulsa el enlace y mira si sobrevive: **0/8 en holdera.es antes, 8/8 después**. El negativo es la prueba de que el verificador sirve.
  - 🔴 **Los arreglos 2 y 3 viven en el FUENTE del producto, que está gitignorado** (`routePath()` en `app/components/hotel/spatialNavigation.ts`, y sus dos llamadas en `RoomsStage.tsx` y `OperationsStage.tsx`). Al repositorio solo llega su EFECTO, la carpeta `panel/` capturada. Si esa carpeta de producto se pierde, el arreglo se pierde con ella y volver a capturar reintroduce el fallo. Es el mismo aviso que ya estaba en la lista de pendientes sobre versionar el producto aparte, ahora con un caso concreto detrás.

- Idioma del contenido: **español**, registro **tú** en toda la página. (Alex no ha confirmado si quiere también EN.)
- Web REAL, no demo: nada inventado. Todo lo no confirmado lleva `<!-- TODO -->` en `index.html` y está en la lista de abajo.
- Datos confirmados por Alex (07-09-2026): "más de 5 años de experiencia", sede en Barcelona.

## Stack
- Vanilla HTML/CSS/JS, sin build. GSAP 3.12 + ScrollTrigger (cdnjs) como capa opcional: la página funciona sin ellos (hero sticky por CSS). **Desde el paso 5 no hay `<script>` de GSAP en el HTML: los inyecta `script.js`** (`loadGsap`) solo con `min-width: 901px` y sin `prefers-reduced-motion`, y `initScroll` arranca cuando han llegado los dos archivos Y ha terminado la entrada, lo que ocurra más tarde.
- Fuentes: Geist + Geist Mono **alojadas en `assets/fonts/`** (`@font-face` al principio de `styles.css`, `font-display: swap`, preload del subconjunto latin en cada página). Regenerar con `node _build/fetch-fonts.js`; aplicar a una página nueva con `node _build/migrations/patch-fonts.js pagina.html`.
- Siete páginas estáticas que comparten `styles.css` (+ `pages.css` las interiores). La cabecera, la isla+drawer y el pie están COPIADOS en cada página desde `_build/shell/*.html` — `node _build/check-shell.js` falla si alguna copia difiere.
- Servidor local de verificación: `node _build/serve.js 4177`.
- **Puertas antes de publicar** — `node _build/gates.js` las pasa todas de una vez, y `node _build/gates.js --fix` reinyecta el SEO, regenera el sitemap y sella los assets antes de comprobar. Una a una (todas salen con 1 si algo falla): `node _build/check-tokens.js` · `node _build/contrast.js` · `node _build/check-shell.js` · `node _build/seo/validate-meta.js` · `node _build/check-placeholders.js --strict` (esta última solo cuando Alex haya dado los datos). SEO: `node _build/seo-inject.js` es idempotente y se puede repetir tras cualquier cambio de `<head>`.
- `_build/extract-logo.js`: saca los PNG del logo (blanco/negro, fondo transparente) a partir de los JPEG originales de WhatsApp. Necesita `sharp` (usar `NODE_PATH` de otro proyecto que lo tenga).

## Tokens de marca (solo en `:root` de `styles.css`)
- Fuente: **Geist** + **Geist Mono** (Google Fonts, cargadas **después** de `styles.css`). Fallback: system-ui.
- Hero claro: `--bg #e5e5e5`, `--text #141414`, `--text-muted #666`, `--border rgba(20,20,20,.10)`.
- Tinta (botones, marcas, barra): `--ink #101010`.
- Acento: `--accent #f08a24`, `--accent-hover #ff9a37`. **El acento no se usa como color de foco** (contraste insuficiente sobre el gris): el foco es `--ink` sobre el hero claro y `--sh-text` sobre la hoja oscura.
- **`--accent-ink #f59b45`** — el acento para TEXTO y bordes de foco/error **sobre la hoja**. Su SIGNIFICADO no cambia con la inversión, su VALOR sí: en la hoja clara era `#a8530a` porque `#f08a24` medía 2,42:1; en la oscura el fallo es el contrario (`#a8530a` mide 2,5:1 sobre `#121110`), así que se aclara — 8,67:1. Todo lo decorativo (puntos, halos, pulsos, el rim de los peces) sigue usando `--accent`.
- **`--accent-hero #9c4c08`** — el acento como TEXTO sobre el **hero claro**. Existe porque `.stars` era el único consumidor de `--accent-ink` que no estaba en la hoja; ver la trampa correspondiente. **Oscurecido de `#a8530a` a `#9c4c08` el 08-09-2026**: `#a8530a` mide 4,27:1 sobre el gris del hero — vale para `.stars` (decorativo, 3:1) pero NO para el hover de la lista de servicios, que es texto de 18px y debe 4,5:1. `#9c4c08` da 4,83:1 y mejora a los otros dos consumidores (estrellas 4,27→4,83; el tic blanco de la casilla de consentimiento 5,38→6,09). Lo cazó `_build/contrast.js`.
- **Tejido Perlin**: `--wave-line` (el hilo sobre el hero claro) y `--wave-line-on-dark` (sobre el escaparate), más `--wave-mask`. Los lee `waves.js`. **El hilo tiene que ser más CLARO que el fondo del hero** — ver la trampa.
- **Escaparate**: `--sc-veil` (el telón que oscurece el hero), `--sc-title-scrim` (la sombra que le compra contraste al título grande) y `--sc-img-shadow`.
- **La hoja es OSCURA desde el 08-09-2026.** `--sh-bg #121110`, `--sh-surface #1c1a17`, `--sh-text #f5f3ef`, `--sh-text-2 #d2ccc2`, `--sh-muted #9c958b`. Nunca `#000000`. Tres tokens nuevos que no existían en claro: `--sh-control-border` (contorno de un control, 3:1), `--sh-bevel` (el realce superior que sustituye a la sombra como elevación) y `--accent-hero` (el acento como texto sobre el hero claro).
- Hoja clara `--sh-*` (histórico) (antes `--dk-*`, renombrados al aclararla): `--sh-bg #f2f1ef`, `--sh-surface #ffffff`, `--sh-surface-2 #fbfbfa`, `--sh-text #131313`, `--sh-text-2 #3c3a38`, `--sh-muted #5f5c58`.
- **`--on-ink-*`** — las superficies que siguen siendo oscuras sobre una página clara: drawer lateral, isla flotante, barra del hero y la terminal de la tarjeta 03. Si algo va sobre tinta, usa este grupo y no `--sh-*`.
- `--brand-*` (8): solo para el color de marca en hover de la tira de partners. Los genera `_build/build-partner-logos.js` junto con el marcado.
- Capas alfa (`--w-*`, `--k-*`, `--accent-a*`, `--panel`, `--bar-a/b`): todo color translúcido vive ahí; **ningún literal de color fuera de `:root`** (los SVG inline usan clases `.viz__*`).
- Easing: `--ease cubic-bezier(.32,.72,0,1)` entra; `--ease-in` sale (drawer, submenú, isla). Duraciones: `--t-fast 200ms` micro, `--t-mid 420ms` paneles.
- Radios: 32/24/16/10 + pill; hoja oscura `--r-sheet 40px` (28px móvil).
- Botones: un solo sistema de 3 variantes (`.btn--primary`, `.btn--secondary`, `.btn--ghost`) + modificadores `.btn--sm`, `.btn--icon`, `.btn--on-dark`. Los pills de MENÚ y el cierre del drawer son `.btn`.
- Espaciado en rejilla 4/8px. **Excepciones con nombre:** el bisel doble de 6px (tarjetas/isla) y la coreografía de entrada del hero (900/1100ms) como momento premium de carga.
- **Pie (paso 5)**: tres columnas (`.footer__grid`: marca, mapa del sitio, contacto) y línea legal. Los datos de contacto son `[data-placeholder]`: mono, subrayado discontinuo, `--sh-muted` — **nunca** restilizarlos para que parezcan datos reales.
- **Páginas interiores**: `pages.css` define sus tokens propios con prefijo `--pg-*` en su `:root`. `check-tokens.js` escanea los dos CSS y todas las páginas.
- **Retirados en el paso 5** (con sus consumidores): `--fish-core`, `--fish-rim`, `--ring-line`, `--ring-echo`, `--w-09`, `--r-sheet-sm`. El CSS que los usaba está aparcado en `_build/parked/fish-and-features.css`. `--sh-surface-3`, `--w-40` y `--scrim` volvieron una temporada porque los leía el panel demo retirado; en el paso 9 se fueron del todo.

## Archivos
- `index.html`, `styles.css`, `script.js`
- `nosotros.html`, `partners.html`, `contacto.html`, `aviso-legal.html`, `privacidad.html`, `404.html` + `pages.css` — las páginas interiores (paso 5). Anatomía: banda clara `.phead` (misma paleta que el hero, con la cabecera absoluta encima y el `.hero-sentinel` abajo para la isla) → `main` oscuro que la solapa 48px → pie. Cargan `script.js` (todos sus bloques se activan por presencia de nodo) y **no** `waves.js`.
- `_build/shell/header.html`, `drawer.html`, `footer.html` — LA copia de referencia de la cabecera, la isla+drawer y el pie. Para cambiar la nav: se edita aquí, se copia a las siete páginas (en `index.html` las anclas de la propia página van sin `index.html`) y se pasa `check-shell.js`.
- `_build/check-shell.js`, `check-placeholders.js` — guardias nuevos (ver Stack).
- `_build/png-probe.js` (paso 8) — mide contraste WCAG sobre los PÍXELES de una captura; decodifica el PNG a mano con `zlib`, así que no necesita `sharp` (que ya no está en la ruta que decía este archivo). Es lo único que puede medir texto sobre un canvas WebGL o sobre una foto. **Ojo**: en texto pequeño sobre fondo plano mide el antialiasing y da falsos suspensos — ver trampas del paso 8.
- `_build/version-assets.js` (paso 8) — sella cada css/js de las 8 páginas con `?v=<sha1 de su contenido>`. Idempotente y re-ejecutable. **Pásalo tras cualquier cambio de css o js**: sin él, el CDN sirve el CSS viejo junto al HTML nuevo durante 7 días.
- `_build/migrations/patch-mobile-deck-art.js`, `patch-placeholder-spans.js` — las migraciones del paso 8 (una sola ejecución; fallan si se repiten).
- `_build/seo/` (`meta.json`, `validate-meta.js`, `build-og.js`, `build-icons.js`, `build-sitemap.js`, `og-*.html`, `CHECKLIST.md`) + `_build/seo-inject.js` — la capa SEO. Los `<head>` llevan un bloque entre `<!-- seo:head -->` y `<!-- /seo:head -->` que el inyector reescribe; **no editar ese bloque a mano**, se edita `meta.json`.
- `assets/fonts/*.woff2` + `_build/fetch-fonts.js` + `_build/fonts/geist-faces.css` — Geist local.
- `assets/seo/og-default.png` (1200×630) · `assets/icons/*` · `site.webmanifest` · `robots.txt` · `sitemap.xml` · `.nojekyll`.
- `assets/img/producto/*.webp` (20 archivos, **805 KB**) — la obra del escaparate del hero: un fondo a pantalla completa y tres paneles por cada una de las cinco pantallas del producto. **Son capturas del panel ya rediseñado**, no fotos de banco: se generan con `node _build/producto-art.js` a partir de cinco capturas de página entera guardadas en `assets/img/producto/_raw/<clave>-full.png` (gitignoradas). Se cargan en diferido por `data-bg` cuando el puntero entra en la LISTA. Las fotos de Pexels del paso 5 (`assets/img/servicios/`) y su generador `_build/service-art.*` se borraron en el paso 10: ya no había nada que las usara.
- `_build/parked/fish.js` + `fish-and-features.css` — la sección «Qué hacemos» (peces + aros de texto), las tarjetas `.feature`/`.viz` y la terminal `.term`, **fuera de la página desde el paso 4** y aparcadas aquí en el 5 (con sus tokens retirados de `:root`). `assets/model/fish-*` y `_build/fish-extract.py` se quedan por si vuelve.
- `_build/migrations/patch-step5.js`, `patch-step5b.js`, `patch-fonts.js` — las migraciones del paso 5 (una sola ejecución; fallan si se repiten).
- `waves.js` — el tejido de líneas Perlin del hero. canvas2d a mano, sin CDN. Puerto fiel del algoritmo de la referencia (Grad/dot2/perlin2/fade/lerp y el modelo de fuerza de `movePoints`); lo que cambia es el color, la densidad y la máscara. Expone `window.HolderaWaves.setTone(bool)` para que el escaparate le cambie el hilo de tinta a claro.
- `_build/producto-art.js` — el conversor de esas capturas: recorta cada PNG de página entera en los cuatro encuadres (fondo 1600x1067, panel vertical 694x1040, dos horizontales 1040x694) por FRACCIONES del alto, no por píxeles, porque las cinco pantallas miden alturas muy distintas. Necesita `sharp` vía `NODE_PATH` (lo trae el producto).
- `_build/shoot.js` (paso 11) — capturas por lotes contra el Chrome de trabajo por CDP, en su propia pestaña: lista de rutas, varios viewports, claro y oscuro, `--eval`/`--eval-file` para medir algo en cada página y `--no-full` para solo el viewport. Es lo que hace posible verificar 18 pantallas × 2 esquemas × 2 tamaños sin pedir captura a captura.
- `_build/a11y-probe.js` (paso 11) — auditoría de contraste EN LA PÁGINA: compone cada texto sobre su pila real de fondos (los `--label-*` llevan alfa y el fondo casi nunca está en el propio elemento) y lista los que no llegan a 4,5:1 (o 3:1 si el texto es grande). Se pasa con `--eval-file`.
- `_build/showcase-contrast.js` (paso 11) — mide los cinco títulos del escaparate del hero sobre la obra, poniéndolos transparentes primero. Hay que repasarlo cada vez que cambien las capturas del panel.
- `_build/lcp-bench.js` (paso 11) — LCP/FCP/CLS medianos por URL con la misma emulación, para comparar dos versiones sin que una traza suelta decida.
- `_build/producto-shots.js` (paso 11) — las cinco capturas en bruto del escaparate, desde el panel ya capturado. La de trazabilidad abre el cajón de Evidence antes de disparar.
- `_build/nav-check.js` (paso 12) — comprueba CÓMO navega el panel, no solo que navegue: pone una marca en `window`, pulsa el enlace y mira si el documento sobrevivió (navegación blanda) o se recargó (dura), más si la escena es el mismo nodo del DOM y si la cámara cambió de nivel. Ocho pasos. Se pasa contra `http://localhost:4177` o contra `--base https://holdera.es`.
- `_build/gates.js` — pasa las cinco puertas de una vez; con `--fix` reinyecta el SEO, regenera el sitemap y sella los assets antes de comprobar. `_build/replicate-shell.js` copia la cabecera/drawer/pie de `_build/shell/` a las siete páginas (lo que `check-shell.js` solo comprueba), y `_build/shell/build-drawer.js` genera el drawer, que son 56 URLs de tesela por página escritas a mano.
- `_build/measure.html` — sonda de contraste sobre píxeles REALES de una captura. Es lo único que puede medir el título del escaparate, que se lee sobre cinco obras distintas con `filter` y scrim encima.
- `_build/frames.html` — extractor de fotogramas de los mp4 de referencia que pasa Alex.
- `_build/hero-fit.js` — **la comprobación que faltaba.** Mide, en el viewport actual, que el copy y la lista de servicios no se crucen, que la lista no invada la barra inferior y que `elementFromPoint` sobre los dos botones del hero devuelva los botones. Pásalo por 1920x1080, 1440x900, 1366x768, 1280x700 y 1366x620 antes de dar por bueno cualquier cambio en la columna izquierda del hero.
- `fish.js` (**aparcado en `_build/parked/` desde el paso 5**; ya no lo carga ninguna página) — el embudo de peces y aros de texto de "Qué hacemos". WebGL a mano. Los peces son WebGL; **los aros son TEXTO REAL del DOM**, colocados con CSS 3D: así son seleccionables, indexables, los lee un lector de pantalla y siguen ahí sin JS. `setScene()` deriva `--persp` y `--u` del mismo foco que la proyección GL para que ambas capas coincidan en 3D.
- `planet.js` — el planeta que gira con el scroll en "Cómo trabajamos". Esfera UV generada en JS, una luz direccional igual que la de la referencia, y el suavizado de Lenis sustituido por un lerp exponencial hacia el objetivo de scroll.
- `assets/model/fish-*.bin` + `fish-scene.json` (**33 KB**): los seis peces del FBX son **seis copias de una sola malla** (1694 vértices), así que se sube una vez y se dibuja seis veces con una matriz por instancia.
- `assets/model/brain-nrm.bin` + `brain-shade.bin` (50 KB): normal y curvatura por punto, calculadas desde la propia nube.
- `assets/img/planet-color.jpg` (287 KB) + `planet-clouds.jpg` (123 KB): el mapa equirectangular re-tintado a la paleta de Holdera. Las nubes viajan en **escala de grises** y se usan como máscara alfa, así no hay que transportar canal alfa.
- `brain.js` — el holograma. WebGL a mano, **sin three.js y sin CDN**: es una sola nube de puntos estática con una pose atada al scroll, así que la librería (~600 KB) no se paga. Degradación en cascada: sin JS / sin WebGL / si falla el fetch → se ve el póster.
- `assets/model/brain-points.bin` (60 KB, int16 XYZ) + `brain-sizes.bin` (10 KB, uint8) + `brain-points.json` (meta). **70 KB en total contra los 11,8 MB del FBX.**
- `assets/img/brain-poster.png` — 768×768, PNG con paleta, 36 KB. Es el respaldo, y se carga como **fondo CSS** (no `<img>`) para que un navegador que sí pinta la nube no lo descargue nunca. Verificado: 0 peticiones cuando WebGL funciona.
- `assets/logo/` — logo completo y monograma (blanco/negro, PNG transparente) + `favicon.svg`
- `assets/img/robot.svg` — el busto anterior. **Ya no se usa** (lo sustituyó el cerebro); se conserva por si Alex quiere volver a él.
- `brain-hologram/source/Brain.fbx` — el FBX original que subió Alex. Fuente, no se sirve.
- `.screenshots/brain-hologram/` — evidencia de verificación (git-ignored)
- `_build/` — servidor local, extractor de logo, y ahora:
  - `brain-extract.py` — Blender headless: FBX → nube de puntos
  - `build-partner-logos.js` — logotipos de partners desde simple-icons
  - `brain-poster.html` + `save-poster.js` — render y compresión del póster
  - `fish-extract.py` — Blender headless: FBX de los peces → malla + posición de los aros
  - `brain-shade.js` — normales y curvatura (gyrus/sulcus) de la nube del cerebro
  - `planet-textures.js` — re-tinta el mapa del planeta a la paleta y lo baja a 2048×1024
  - `contrast.js`, `check-tokens.js` — verificadores (WCAG y sistema de tokens). **Los dos leen los archivos reales y salen con código 1 si algo falla**, así que sirven de puerta antes de publicar. `contrast.js` nombra selector + propiedad y saca el valor de `styles.css`: si renombras una regla que vigila, **da ERROR** en vez de pasar en silencio.

## Publicación (paso 7, 09-09-2026)

- 🔴 **El dominio es `holdera.es`. `holdera.com` NO es de Alex.** Está registrado por un tercero desde 2017: resuelve a `64.99.64.32` (Tucows, no Hostinger), devuelve 404 por HTTP, no responde por HTTPS y tiene correo activo (`mx.holdera.com.cust.b.hostedemail.com`). Durante los pasos 1–6 todo el SEO lo declaraba canónico. Si vuelve a aparecer `holdera.com` en cualquier sitio, es un error.
- **Repositorio**: `alexrickfelderroca/holdera`, **público**, rama `main`. Cuenta `alexrickfelderroca`, nunca `trvevr2-a11y`.
- **Despliegue**: sitio de tipo **PHP/HTML** en hPanel → `Avanzado` → `GIT` → repo `holdera`, rama `main`, directorio `public_html`. **No compila nada**: clona el repositorio y sirve los archivos. La implementación automática está activada: cada `git push` publica en segundos.
- **`.htaccess`** (en la raíz del repo): `ErrorDocument 404 /404.html`, `Options -Indexes`, deniega `_build/` y los `.md`, redirige `www` → sin www, y pone cabeceras de caché. **No** lleva redirección a https ni compresión porque Hostinger ya hace las dos (verificado en vivo: 301 y `Content-Encoding: br`); `/.git/` ya devuelve 403 por su cuenta.
- **URL de vista previa** (sirve aunque el dominio no apunte): `mediumspringgreen-ape-800901.hostingersite.com`. IP del sitio: `91.108.101.228`.
- **`HOLDERA_SHARP_PATH`**: `build-og.js` y `build-icons.js` toman `sharp` prestado de otro proyecto. La ruta se pasa por variable de entorno, ya no escrita en el código (el repo es público). Hoy vale `holdera-product-hotel-operations-v1/holdera-product-hotel-operations-v1/node_modules` (el producto trae sharp con Next 16). La ruta anterior, `…/aplomo/site/node_modules`, ya no existe.

### Trampas de la publicación

- 🔴 **Hostinger manda el repositorio al desplegador de Node.js en cuanto encuentra UN `package.json`, esté donde esté.** El único del repo era `_build/logos/package.json` —el instalador de un solo uso de simple-icons—, declaraba `"main": "index.js"` (archivo que nunca existió) y además hizo que Hostinger pusiera el **directorio raíz del proyecto en `_build/logos`**. Resultado: `npm install` correcto y despliegue fallido acto seguido. `_build/logos/` está ahora en `.gitignore`; el comando para recrearla está en la cabecera de `_build/build-partner-logos.js`.
- 🔴 **El producto «App web de Node.js» NO puede servir un sitio estático, y no hay forma de configurarlo para que lo haga.** Su desplegable de preajustes son 19 opciones y **ninguna es estática** (Angular, Astro, CRA, Express, Fastify, Gatsby, Hono, NestJS, Next.js, Nitro, Nuxt, Other, Parcel, React, React Router, Svelte, SvelteKit, Vite, Vue.js), y el campo «Archivo de entrada» es **obligatorio**. Un sitio estático va en **PHP/HTML**, que es el único tipo con `Avanzado → GIT`. Se perdió una hora configurando la pantalla equivocada.
- 🔴 **El `.htaccess` va en el REPOSITORIO, jamás creado a mano desde el gestor de archivos de hPanel.** Un archivo creado a mano dentro de `public_html` hace que el `git pull` del despliegue aborte por conflicto y **el sitio se queda congelado en el commit viejo sin avisar**.
- **El CDN de Hostinger (`Server: hcdn`) cachea, y una regla nueva del `.htaccess` no se nota hasta purgar.** Tras el primer despliegue con el `.htaccess`, `_build/serve.js` seguía devolviendo 200 mientras que con `?nocache=1` ya daba 404. Se purga con «Borrar caché» en el panel del sitio. **Verificar siempre con cache-buster antes de dar por buena una regla.**
- **`404.html` lleva `<base href="/">`** y su enlace de salto apunta a `/404.html#contenido`. Como `ErrorDocument` se sirve desde CUALQUIER ruta, sus rutas relativas se resolvían contra la carpeta pedida (`/loquesea/styles.css`) y la página salía sin estilos. No se tocan los `href` de la cabecera, el drawer y el pie porque están COPIADOS y `check-shell.js` los compara normalizando `href="index.html#..."`, que una ruta absoluta rompería.
- **Los marcadores de contacto ya no son enlaces.** Tenían `href="https://instagram.com/PENDIENTE"` y `href="https://wa.me/PENDIENTE"`: URLs reales de terceros. Se quitaron los 37 `href` de las 7 páginas y de `_build/shell/footer.html`. El texto y los `data-placeholder` siguen ahí y `check-placeholders.js` sigue listando los 74.
- **El formulario ya no dice «Recibido».** Sigue sin backend, pero el estado de éxito dice la verdad. Conectarlo es el TODO número uno.

## Decisiones de scroll (paso 1)
- `.hero-stage` mide 250dvh; el hero es `position: sticky` y `main` sube con `margin-top: -100dvh` como hoja clara que se desliza por encima.
- Con GSAP (escritorio, sin `prefers-reduced-motion`): el copy y el aside **suben** y se atenúan, el wordmark **baja**; la nav se apaga justo antes de quedar cubierta.
- 🔴 **`.hero__figure` NO está en la línea de tiempo de GSAP, a propósito.** El cerebro lo mueve `brain.js` **dentro de la escena WebGL** (deriva hacia abajo, giro y desvanecido). Dos escritores sobre un mismo `transform` se pelean. Si hay que tocar el movimiento del cerebro, se toca `pose()` en `brain.js`, nunca un tween nuevo sobre `.hero__figure`.
- `brain.js` **mide el scroll por su cuenta** (`readProgress()` sobre `.hero-stage`), no lo recibe de GSAP: así la coreografía del cerebro sobrevive a un CDN caído.
- La línea de tiempo se crea 1,5 s después de terminar la entrada (evita que GSAP capture el estado pre-entrada) y usa `fromTo` con valores explícitos. En móvil (≤900px) no hay pin: flujo normal.
- Revelados laterales (`.reveal[data-reveal=left|right|up]`) con IntersectionObserver + barrido por `getBoundingClientRect` + barrido periódico; todo gateado por `.has-js`.
- La isla flotante se decide **por posición en el scroll**, no con IntersectionObserver: un salto de scroll puede cruzar el centinela sin llegar a intersecarlo nunca.
- Cuando la hoja tapa el hero en escritorio, el hero pasa a `inert` (no se puede tabular a algo invisible). La nav está FUERA del hero desde el paso 6 precisamente por esto: fija dentro de un hero `inert` no se podría usar.
- 🔴 **La nav fija no está en la línea de tiempo de GSAP** (paso 6). Antes se apagaba al 82 % del escenario; ahora la visten las clases `is-scrolled` / `is-over-sheet` / `is-veiled` desde `script.js`. Si se vuelve a tuenear `.nav`, vuelve el bug de la nav apagada.
- **Enlaces profundos al deck (paso 5).** Cada diapositiva tiene `id="servicio-<x>"`. Con JS las cinco están apiladas en un viewport pegajoso, así que un salto de ancla nativo cae en la parte alta del escenario y enseña la diapositiva 0. `script.js` intercepta los clics a `#servicio-*` de la MISMA página y desplaza a `stage.top + travel · k/(n−1)`; con hash al cargar (enlace desde otra página) corrige dos veces (0 y 400 ms) porque el escenario se asienta tarde. Sin JS o con movimiento reducido las diapositivas están apiladas de verdad y el ancla nativa sirve.
- **`onLeaveBack` + `.is-settled` (paso 5).** Al volver por encima del inicio del escenario, `onLeaveBack` termina el tween del scrub al instante (`self.getTween().progress(1)`) para que nav, copy y aside estén en reposo en el mismo frame en que se quita `is-scrolling`; y 2,1 s después de `is-ready` el hero recibe `is-settled`, que apaga las transiciones de `.nav` y `.hero__wordmark` — los dos elementos que GSAP escribe — para siempre. Sin esto, el scrub escribía valores intermedios inline mientras la transición de 900 ms de la entrada estaba rearmada, y la nav tardaba hasta 1,8 s en reaparecer arriba.
- 🔴 **`scroll-behavior: smooth` y `ScrollTrigger.refresh()` no se llevan bien (paso 5d).** En `_refreshAll` el plugin anota `rec = scrollY`, pone `scroll-behavior: auto` inline y llama a `scrollTo(0)` en la misma tarea, sin forzar estilos; Chrome no aplica ese `auto` a tiempo, el `scrollTo` desliza suave, la caché de ST cree que ya está a 0 y mide el escenario en la posición real → `start = −scrollY`. Con la página abajo, todo es progreso 1 y la nav queda apagada hasta recargar; el deslizamiento pendiente encima «sube» la página solo. Es el bug que Alex describió. `initScroll` lo neutraliza en `refreshInit` (`auto` inline + `void html.offsetHeight`) y usa `start: 0` / `end: () => stage.offsetHeight − innerHeight` (números: sin medición por rect). Si algún día se quita el `smooth` del CSS, este apaño sobra; mientras tanto, **no lo quites**. Quien lo cazó lo hizo leyendo el plugin sin minificar — la versión minificada no deja ver el `scrollTo(0)`.
- **`clearScrollMemory()` + `heal()` (paso 5c)** son la red de seguridad: ST olvida la posición guardada por URL, re-mide en el siguiente frame / `load` / fuentes, y en cada scroll con `scrollY ≤ 2` comprueba el disparador (`tl.scrollTrigger.progress` y `.start`), no la clase — `onUpdate` ya rechaza `is-scrolling` arriba, así que la clase está limpia mientras los valores inline siguen siendo los viejos. Si el disparador está mal: `refresh()` y línea de tiempo a 0. Un `refresh()` revierte y vuelve a ejecutar el contexto de `matchMedia` (nueva línea de tiempo): por eso el callback devuelve una limpieza que quita el listener de `heal` y la clase.

## Trampas del paso 3 (inversión a oscuro, peces, planeta)
- 🔴 **Invertir un token no es invertir a todos sus consumidores.** `--accent-ink` tenía diez usos: nueve en la hoja y **uno en el hero claro** (`.stars`). Al voltear su valor a `#f59b45` para la hoja oscura, las estrellas del hero cayeron a **1,73:1** y `contrast.js` lo cazó. Por eso existe ahora **`--accent-hero: #a8530a`** — el acento como texto sobre el gris del hero. Antes de voltear un token, `grep` sus usos y separa los que están del otro lado de la frontera claro/oscuro.
- 🔴 **`accent-color` en la casilla de consentimiento va aparte.** Pinta un check BLANCO sobre el relleno: con `#f59b45` el check queda a ~1,9:1. Esa regla usa `--accent-hero` (check blanco a 5,38:1) y **no** debe entrar en ningún barrido de `--accent-ink → --accent`.
- **En oscuro, la elevación es un bisel arriba, no una sombra abajo.** `--shadow-sh` empieza por el bisel (`--sh-bevel`); una sombra negra sobre un fondo casi negro no oscurece nada. Es el espejo exacto de la nota de la hoja clara ("un realce blanco sobre una superficie casi blanca no existe").
- **El borde de un CONTROL no es el borde decorativo.** `--sh-border-strong` compone a 1,95:1 y vale para el pill `.tag`; un botón fantasma cuyo único indicio es su contorno necesita 3:1 → **`--sh-control-border` (0,36)**, el escalón más bajo que pasa contra los cuatro fondos de la hoja. (También fallaba en la hoja clara, a ~1,5:1: es un arreglo, no una regresión.)
- **En móvil no hay solape: la costura claro→oscuro es una línea recta.** Por debajo de 901px `main{margin-top:0}` y el hero deja de estar pinado, así que el gris del hero choca con el casi negro. Los tokens `--seam-1/2/3` y la rampa de 200px en `.sheet--intro` dentro de la media query son lo que cumple el mínimo de la casa (≥3 paradas o ≥120px). En escritorio no hace falta: la hoja **se superpone** como tarjeta con radio y sombra.
- 🔴 **El cull por orientación borra el 80% de la nube.** Medido sobre el cerebro real: la orientación media es 0,202 y solo el **19,6%** de los puntos miran a cámara por encima de 0,5 — es lo que hace cualquier superficie cerrada. Con un exponente >1 el cerebro se deshace en polvo; por eso el exponente es **0,85** (menor que 1) y `uScale` se multiplica por 1,42 cuando hay sombreado. Si tocas uno, toca el otro.
- 🔴 **Un punto en el pliegue va más GRANDE, no más pequeño.** Ya es más oscuro y está más lejos; encogerlo además dejaba el surco despoblado, y una zona rala de puntitos oscuros sobre fondo claro se lee **más clara** que su entorno — o sea, el pliegue al revés.
- **`uShaded` es el único uniform declarado en los dos shaders**, y las dos etapas tienen precisión por defecto distinta (`highp` en vértice, `mediump` en fragmento). GL **rechaza el enlace** si no coinciden: va cualificado a mano en ambos. El síntoma es el póster de respaldo sin más explicación.
- **CSS y WebGL no comparten el eje Y.** En GL +Y sube; en CSS +Y baja. `fish.js` alinea las dos escenas derivando `--persp` y `--u` del mismo foco que usa la proyección GL (la derivación está sobre `setScene()`), y **niega el pitch** y la Y al colocar. Si el texto de los aros aparece visto desde abajo mientras los peces se ven desde arriba, es este signo.
- **`fitWords()` necesita `--u`, y `placeRings()` corre antes que `boot()`.** La metadata resuelve antes de que nada haya llamado a `setScene()`, así que `fitWords` se salía por su propio guardia y `--k/--n` no se escribían nunca: todas las copias de todas las frases se apilaban en el ángulo 0. `placeRings` llama a `setScene` de entrada por eso.
- **Dos reglas con la misma especificidad: gana la última.** `.ostep:focus{outline:none}` (para evitar un doble anillo) mataba a `.ostep:focus-visible{outline:…}` porque iba después y ambas suman (0,3,0). El foco desaparecía por completo y solo se veía con un Tab real, no con `.focus()` desde consola.
- **`.focus()` desde JS no dispara `:focus-visible`.** Para comprobar un anillo de foco hay que **pulsar Tab de verdad**; medirlo tras un `.focus()` programático da `outline-style: none` y parece un fallo que no existe.
- **`scroll-behavior: smooth` falsea las mediciones.** Fijar `scrollingElement.scrollTop` y leer al instante devuelve la posición vieja: hay que poner `scrollBehavior='auto'` antes de verificar, o esperar. Perdí una ronda creyendo que el listener de scroll estaba muerto.
- **`check-tokens.js` sabía de `var()` y nada más.** `brain.js`/`fish.js`/`planet.js` **leen** tokens con `getPropertyValue` y **escriben** propiedades con `setProperty`, así que el guardia daba orphans y undefined falsos sobre el patrón correcto. Ahora cuenta cualquier literal `'--x'` en los scripts como uso, y los `setProperty` como definición. Probado rompiéndolo a propósito en las tres direcciones (orphan, token borrado que solo usa JS, literal de color fuera de `:root`).
- **La textura del planeta es CYAN de origen.** Se re-tinta **en build** (`_build/planet-textures.js`, rampa por luminancia) y no en el shader: un tinte por fragmento se paga siempre y un PNG re-tintado se puede mirar. El mapa es 4096×4096 pero su contenido es equirectangular 2:1, así que bajarlo a 2048×1024 **quita** el estirado, no lo añade.

## Trampas del paso 4 (Perlin, cerebro reactivo, escaparate)
- 🔴🔴 **UNA captura a 1440x900 no verifica un hero.** El copy y la lista de servicios se posicionaron por separado — el copy anclado ARRIBA, la lista ABAJO — y a 1440x900 se libraban por 31px. Por debajo de ~896px de alto se cruzaban, y la lista (z-index 4) se comía los clics de los DOS botones del hero (z-index 2): a 1366x768, `elementFromPoint` en el centro de «Habla con nosotros» devolvía el `LI` de «Asesorías 1:1». **Es la misma trampa que el `fov` fijo del cerebro: el fallo no aparece en la captura de referencia.** Arreglo: `.hero__left`, una sola columna flex con `justify-content: space-between`, así no pueden cruzarse a ninguna altura. **Verifica siempre con `_build/hero-fit.js` en los cinco viewports, no solo en uno.**
- 🔴 **Las primeras media queries de ALTO del archivo, y el umbral es derivado, no elegido a ojo.** La columna necesita `top + copy(382) + gap(28) + lista(206) + fondo(92) <= H`, y en ese rango `top` es `0.21H`, así que encaja mientras `0.21H + 708 <= H`, o sea **H >= 896**. Por eso el primer escalón es `max-height: 895px` y no un número redondo. Un intento previo lo puso en 865 «por si acaso» y estaba mal en la dirección peligrosa: a 866 la columna ya desbordaba 24px y la lista libraba la barra por 2px de suerte.
- 🔴 **`pointer-events: none` NO saca nada del orden de tabulación.** El copy y la tarjeta del hero se atenuaban a `opacity: 0` con `pointer-events: none` durante el escaparate, pero sus tres enlaces seguían siendo tabulables: con el ratón parado sobre un servicio, Shift+Tab movía el foco a un botón invisible y no se veía anillo de foco en ninguna parte (WCAG 2.4.7). Lo que hace falta es `visibility: hidden` — que además los saca del árbol de accesibilidad — retrasada `--t-mid` al entrar y a 0s al salir.
- 🔴 **`clip-path` recorta TODO el renderizado del elemento, sombras exteriores incluidas.** `.sc__img` declaraba dos `box-shadow` de caída y ninguna se dibujaba nunca: incluso abierto del todo, su polígono es exactamente la caja de borde. En un elemento recortado la única sombra que sobrevive es `inset`.
- 🔴 **Un verificador puede medir el fondo EQUIVOCADO y aprobar un suspenso.** El fondo del hero es un degradado radial de `--bg-2` a `--bg-3` más la viñeta; la lista vive abajo a la izquierda, el suelo más oscuro de la página. `contrast.js` la medía contra `--bg` (la parada MEDIA) y daba PASS 4,83:1 donde el texto renderiza a 3,99:1. Ahora se mide contra `HERO_LOW` = `--k-06` sobre `--bg-3`, el peor suelo que el hero puede producir. Es la misma lección que ya está escrita más abajo sobre la primera versión de `contrast.js`, en otra forma: **no basta con leer el archivo real, hay que leer el sitio real.**
- **Los oyentes de `mousemove`/`touchmove` viven en `window` y hay que apuntalarlos.** `waves.js` medía layout en cada evento aunque el hero estuviera fuera de pantalla — y `touchmove` se dispara sin parar en cada gesto de scroll de un móvil. Van con puerta `if (!running || !booted) return;`.
- **`ResizeObserver` sobre el hero, no `resize` de window.** Por debajo de 901px el hero es `height: auto`: su caja cambia cuando aterriza Geist, sin que la ventana cambie. Con movimiento reducido no hay bucle que vuelva a llamar a `setSize()`, así que el lienzo se quedaba con su tamaño viejo y dejaba una banda sin pintar.
- 🔴 **El hilo del tejido Perlin es BLANCO, y es una decisión de accesibilidad, no de gusto.** El extremo pálido del degradado del H1 (`--text-fade` #7a7a7a) mide **3,41:1** sobre el gris del hero, con el suelo de 3:1 justo debajo. Un hilo de TINTA por detrás de esa palabra baja el fondo local a ~#d5d5d5 y el ratio a **2,93:1** — suspenso. Un hilo BLANCO sube el fondo local y todo el texto del hero acaba con MÁS contraste que antes: 3,41 → **3,94:1**. `_build/contrast.js` lo vigila como INVARIANTE (la dirección, no un número): si alguien vuelve a poner tinta, falla. Probado rompiéndolo.
- 🔴 **La cuña de `clip-path` solo interpola entre polígonos con el MISMO número de vértices.** Los cuatro estados de partida (`right`/`left`/`top`/`bottom`) están escritos como cuatro esquinas colapsadas sobre un lado, nunca como `inset()`.
- 🔴 **`.hero__copy-in` y `.hero__aside-in` existen para que GSAP y el escaparate no escriban la misma propiedad.** GSAP tuenea `opacity`/`transform` de `.hero__copy` y `.hero__aside` con el scroll, y lo hace por estilo EN LÍNEA, que le gana a cualquier regla del CSS sin `!important`. Con un envoltorio, GSAP se queda con el exterior y el CSS del escaparate con el interior: se multiplican en vez de pelearse. Si añades otra capa de animación al hero, hazlo igual.
- **El escaparate es de PUNTERO, y a propósito no responde al foco de teclado.** Es decoración: apagar el H1 porque alguien está tabulando por cinco enlaces castiga justo a quien no puede ver la obra. Teclado = anillo de foco normal; lector de pantalla = cinco enlaces a `#servicios`; móvil = la lista sin escaparate.
- **`pointer-events: none` además de `opacity: 0` en el copy atenuado.** Un botón «Habla con nosotros» invisible se sigue comiendo los clics destinados a la obra que hay debajo.
- **El fondo a pantalla completa del escaparate lleva `filter: brightness(0.38)`.** A plena intensidad se lee tan bien como los tres paneles de delante y acabas viendo la misma obra dos veces a dos escalas. La referencia usa `brightness(0.2)` por exactamente lo mismo.
- 🔴 **`putImageData` NO respeta `globalCompositeOperation` ni `globalAlpha`.** Escribe los píxeles tal cual. El grano del generador de obra pintado así no se superponía: BORRABA la composición entera. Se detectó porque los 20 archivos pesaban en proporción exacta a su número de píxeles (ruido puro = incompresible) y no a su contenido. El grano va por `createPattern` + `drawImage`, y como tesela de 160px: el ruido es lo más caro que puede tragar un webp.
- 🔴 **`_build/serve.js` no responde peticiones `Range`, así que Chrome no puede BUSCAR dentro de un mp4.** Cada `currentTime = t` devolvía el primer fotograma otra vez — seis copias idénticas. `_build/frames.html` REPRODUCE el vídeo y captura al vuelo con `requestVideoFrameCallback`.
- **Para medir el contraste de un título sobre obra gráfica hay que mirar el píxel.** La caja de muestra tiene que ser la de los GLIFOS (un `Range` sobre el nodo de texto), no la celda de rejilla: la celda ocupa todo el ancho del escenario y arrastra obra que las letras no tocan, lo que fabrica un suspenso inexistente. Medido así, el peor píxel detrás de cada título da 12,95 / 5,94 / 7,33 / 5,68 / **3,93:1** — todos por encima del 3:1 que debe un título de 84px.
- **El desplazamiento del cerebro se aplica en espacio de CÁMARA, no de modelo.** El empujón tiene que ocurrir donde está el puntero EN PANTALLA y el cerebro está girando; en espacio de modelo la abolladura viajaría con el giro. Por eso ya no hay `uMVP`: la proyección y el model-view van por separado (`uProj`/`uMV`) y `aDisp` se suma entre las dos.
- **`MAX_DISP` es la barandilla del cerebro.** Los primeros números parecían correctos con el ratón lento y destrozaban la silueta con un gesto rápido. 0,30 sobre una nube que abarca ±1,1 significa que un punto se aparta como mucho un octavo del ancho del cerebro. Coste medido: 144,9 fps en reposo y 144,9 fps con el ratón barriendo la nube (p95 7,4 → 7,6 ms).
- **El navegador de Chrome DevTools MCP se comparte con las demás sesiones.** Durante este paso otra sesión (`serres-web-14`, puerto 4188) cambió el viewport A MITAD de una captura: una salió 1440×780 en vez de 1440×900 y la medida de contraste se fue del sitio. No basta con `emulate` una vez: hay que verificar `innerWidth` en la misma llamada que activa el estado, y medir en coordenadas PROPORCIONALES.

## Trampas del paso 5 (bugs, páginas, SEO)
- 🔴 **`background-clip: text` en un padre NO pinta el texto de un hijo compuesto aparte.** El degradado del H1 vivía en el `h1` y las tres líneas entraban con `transform` (capa propia): durante los ~1,9 s de la entrada las tres líneas eran INVISIBLES y aparecían de golpe al terminar. Lo cazó la captura móvil del paso 1 (caja del título en su sitio, sin glifos). El degradado va ahora en cada `.line > span` (`background-size: 100% 300%` y `background-position` por línea para conservar el barrido del bloque). Regla: el clip de texto va en el MISMO elemento que se transforma.
- 🔴 **Chrome DevTools MCP con varias sesiones/agentes: `navigate_page` actúa sobre la pestaña SELECCIONADA, que puede ser la de otro.** Un agente navegó mi pestaña (`isolatedContext=main-verify`) a `404.html` entre dos llamadas y la captura «del hero» fue del 404. Cada sesión abre su pestaña con `new_page` + `isolatedContext`, hace `select_page` antes de CADA llamada y comprueba `location.href` e `innerWidth` dentro del mismo `evaluate_script` que mide. Con más de dos sesiones a la vez, verificar por turnos.
- **El límite de sesión de la API mata a los agentes en silencio a mitad de tarea.** Tres constructores y 89 verificadores cayeron con 429; al volver, `SendMessage` al mismo id reanuda con su contexto (mejor que relanzar). Guardar los resultados parciales en disco, no en la memoria del agente.
- **Dos spans con la misma palabra y sin espacio se rastrean como una** («EscuchamosEscuchamos»). `aria-hidden` no existe para un buscador. El barrido va en `::before { content: attr(data-word) / "" }`: el `/ ""` deja el texto generado fuera del nombre accesible.
- **Un `<h3>` decorativo dentro del hero rompe el esquema h1→h2 aunque esté oculto**: el escaparate se rastrea. Los rótulos que no son encabezados son `<p>`.
- **El deck oculta 3 de 5 servicios a rastreadores y lectores** (`visibility: hidden` en las diapositivas fuera de escena). No se «arregla» en CSS (las diapositivas opacas NECESITAN ocultarse para no pintarse unas sobre otras): el índice `.svc-index` siempre visible encima del deck es la lista de verdad, con enlace a cada ancla.
- **En móvil la hoja NO lleva radio.** Por debajo de 901px no hay solape: las esquinas redondeadas de `main` dejaban ver el cuerpo oscuro en dos «orejas» entre el gris del hero y el gris de la rampa. La costura es el borde.
- **`clamp(30px, 13vw, 84px)` recortaba «TRANSFORMACIÓN» a 390px**: el título del deck es `overflow: hidden` (máscara de entrada) y a ese tracking la palabra mide ~8,9em → cabe hasta ~39px; ahora es `9.6vw` (37px a 390).
- **El hover de los pasos en móvil no tiene banda**: los colores de escritorio (tinta, para leer sobre naranja) dejaban el título a ~1,1:1 al enfocarlo con teclado. El bloque ≤900px restaura los colores de reposo.
- **`declare()` de `contrast.js` busca selectores INDIVIDUALES**: `'.footer__nav a, .footer__contact a'` no se encuentra aunque exista como lista; hay que vigilar `.footer__nav a`.
- **Las páginas comparten cabecera COPIADA, no incluida**: sin build no hay includes. `check-shell.js` normaliza `index.html#x`→`#x`, quita `is-active`/`aria-current` y los comentarios HTML antes de comparar. Cualquier cambio de nav se hace en `_build/shell/` y se replica.
- **Los marcadores de contacto son visibles a propósito** (`[email por confirmar]`), no datos plausibles: un `hola@holdera.es` inventado se habría publicado como real. `check-placeholders.js` los lista; `--strict` los convierte en puerta.
- **`styles.css` estiliza el elemento `main` a pelo como la hoja deslizante** (`margin-top: -100dvh`, radio, sombra, `overflow: clip`): toda página nueva tiene que sobreescribirlo (`pages.css` lo hace). Un `main` sin override aparece 100dvh más arriba de donde debe.
- **Un `.sr-only` posicionado en absoluto dentro de una tabla con scroll toma como contenedor el contenedor de la app** salvo que el envoltorio con scroll sea `position: relative`; si no, el texto oculto desplaza el ancho de la página.
- **La ENTRADA del panel no lleva `noindex`, y las otras 258 sí.** Ponerle `noindex` a una página de marketing baja Lighthouse a SEO 66, así que `/panel/` se indexa; las 258 páginas de dentro llevan `noindex,follow` porque son la misma demo vista por dentro («Room 407 · Holdera Demo Hotel»), en inglés y de un hotel que no existe. Lo escribe `snapshot-panel.js`, no `seo-inject.js`.
- **Pexels sirve la página de búsqueda a `curl` solo con el UA completo de Chrome + `Accept`/`Accept-Language`**; con un UA corto devuelve «Just a moment» (Cloudflare). Las URLs `images.pexels.com/photos/<id>/…?w=1600` se descargan sin clave. Elegir SIEMPRE a ojo: el `alt` no cuenta la marca del portátil ni el logotipo de ChatGPT que salía en cada «chat en el móvil».

## Trampas del paso 8 (móvil)
- 🔴 **El `.htaccess` cachea css y js SIETE DÍAS y el HTML se revalida siempre.** Tras un despliegue, quien haya visitado la web en la última semana recibe **el HTML nuevo con el CSS viejo**, que es peor que no desplegar. Medido en vivo minutos después del push del paso 8: `https://holdera.es/styles.css` devolvía 87.194 bytes (la versión anterior) y `…/styles.css?x=1` devolvía 96.117 (la nueva). El CDN de Hostinger sirve la copia rancia en la URL canónica y solo va a origen si la URL cambia. **Purgar a mano arregla ese despliegue y se olvida en el siguiente**: por eso existe `node _build/version-assets.js`, que sella cada css/js con `?v=<sha1 de su contenido>`. Es idempotente; **pásalo tras cualquier cambio de css o js**, junto a las otras cuatro puertas.
- 🔴 **Un verificador de píxeles mide el ANTIALIASING, no el color, y da falsos suspensos en texto pequeño.** El eyebrow del hero salía a **2,89:1** midiendo los glifos de una captura y está a **4,99:1** de verdad (`#666` sobre el suelo del hero). Dos agentes dieron por bueno que el lienzo Perlin se pintaba encima del texto y lo causaba; medido con y sin el tejido, **0,00 de diferencia**: el arreglo propuesto no habría hecho nada. `_build/png-probe.js` sirve para texto sobre obra gráfica o sobre un canvas WebGL, donde el fondo varía de verdad. Para texto sobre un fondo plano, la respuesta son los **colores especificados**.
- 🔴 **Dos `@media` con la misma especificidad: gana la que va después en el ARCHIVO, no la más específica en intención.** `@media (prefers-reduced-motion: reduce) { .has-js .deck__stage { height: auto } }` estaba ANTES que `@media (max-width: 900px) { … height: 420dvh }`, así que un móvil con «Reducir movimiento» conservaba 3.545px de escenario para 1.810px de contenido. Se arregla con una consulta **combinada al final del archivo**, nunca acotando el bloque móvil con `and (prefers-reduced-motion: no-preference)`: eso se llevaría por delante el grid de una columna y el `9.6vw` del título, que ese móvil sigue necesitando.
- **`elementFromPoint` NO prueba el orden de PINTADO.** Devolvía el texto del hero por encima del lienzo Perlin, pero el lienzo lleva `pointer-events: none` y el hit-test lo salta aunque se pinte encima. Para saber qué tapa a qué: oculta la capa sospechosa y compara los píxeles.
- **`.focus()` desde JS no dispara `:focus-visible`** (ya estaba escrito para el paso 3) — y la vuelta: **`btn.click()` desde JS tampoco da el foco al botón**. Por eso una prueba del drawer decía que el foco no volvía al MENÚ al cerrar con Escape: `lastFocus` era `<body>`. Con el foco puesto antes, funciona. Era un falso positivo de la prueba, no un bug.
- **Un `::after` sobre un `<input type="checkbox">` no se pinta**: es un elemento reemplazado. Para agrandar su zona táctil se agranda la ETIQUETA asociada por `for`/`id`, no el cuadro.
- **`target-size` de Lighthouse pasa con 24px**, que es el mínimo de WCAG 2.2 AA (criterio 2.5.8). Los 44px son HIG y WCAG 2.5.5 AAA. Un objetivo de 34px **no** es una violación WCAG; es ergonomía. Y los enlaces **en línea dentro de un párrafo** están exentos del criterio: agrandarlos rompe el interlineado.
- **`sharp` ya no está donde dice el CLAUDE.md.** `HOLDERA_SHARP_PATH` apuntaba a `…/aplomo/site/node_modules`, que ya no existe. `_build/png-probe.js` decodifica el PNG a mano con `zlib` y no necesita nada instalado.
- **Medir el cerebro «a ojo» en dos capturas a distinto scroll engaña**: el elemento se desplaza con la página, así que dos capturas parecen distintas aunque la pose sea idéntica. Hay que medir la posición de la nube **dentro de su propia caja** (aislar el canvas ocultando el resto y comparar el centroide).

## Trampas del paso 9 (capturar el producto en estático)

- 🔴 **Las fuentes se referencian DENTRO del CSS, no en el HTML.** Un recolector que solo mire `href=`/`src=` se deja las cinco IBM Plex (`url(../media/…)`, ruta RELATIVA), el producto se pinta con la tipografía de respaldo y **no hay ningún error en consola**. En una captura pasa por bueno. `snapshot-panel.js` rastrea los `url()` de cada hoja y los resuelve contra la carpeta del CSS; ignora `url(#…)`, que son filtros SVG del propio documento.
- 🔴 **Sin `basePath`, React NO hidrata bajo un subdirectorio.** El cliente de Next deriva su ruta de `location.pathname` —`/panel/`—, no la reconoce y se queda sin enganchar: los enlaces siguen yendo porque son `<a>` reales, pero **el botón «Trace» se queda muerto**, que es justo la pieza que demuestra la trazabilidad. Comprobado midiendo `__reactFiber` en el botón: en la raíz aparece, bajo `/panel/` no. Se arregla compilando con `HOLDERA_BASE_PATH=/panel`.
- 🔴 **Pero `basePath` rompe la llamada que la app se hace a sí misma.** Las páginas leen sus datos por HTTP contra sus propias rutas `/api/…` (ver `app/lib/internalApi.ts`); con `basePath` viven en `/panel/api/…`, la llamada da 404 y la página se renderiza **vacía: 17 KB en vez de 420 KB**. En vez de tocar el código del producto, `_build/panel-proxy.js` se pone delante y reenvía `/api/…` a `/panel/api/…`; como la app construye la URL con la cabecera `Host`, al capturar a través del proxy se llama a sí misma a través del proxy.
- 🔴 **Ya hidratado, quien navega es el router de Next con SU url, no el `href` del DOM.** Empujaba a `/panel/rooms` y el archivo está en `/panel/rooms/index.html`: 404. Por eso `trailingSlash: true` va SIEMPRE con `basePath` aquí. Y hay que canonizar las rutas al encolarlas: sin eso se capturan `/rooms` y `/rooms/` como dos páginas y salen **377 páginas y 92 MB** en vez de 259 y 54.
- 🔴 **React reconcilia y BORRA lo que le metas en el HTML.** En el App Router hidrata el documento entero: el enlace de vuelta al sitio, inyectado dentro de la barra lateral, estaba en el HTML y no aparecía en pantalla. Se crea **desde JavaScript después de hidratar**, y un `MutationObserver` lo repone si una navegación rehace el árbol.
- 🔴 **Un crawl ingenuo explota por las queries.** El conmutador de fecha/hora es UN parámetro en el servidor y UN ARCHIVO POR COMBINACIÓN en estático: 60 fechas × 3 horas × 100 habitaciones. Medido: **8.060 URLs y 649 MB en solo 2.000 páginas** antes de parar. `queryAllowed()` es la puerta; sin ella no hay captura posible.
- **El sitemap se regenera desde `meta.json`, no desde el archivo.** `/panel/` entró en `sitemap.xml` por una sustitución de texto, así que el siguiente `build-sitemap.js` lo habría borrado **en silencio**. Ahora vive en `meta.extraSitemap`, una lista aparte para URLs que van al sitemap pero no son páginas del sitio (no llevan el bloque `<!-- seo:head -->`).
- **Git Bash convierte cualquier argumento que empiece por `/` en una ruta de disco** (`/panel` → `C:/Program Files/Git/panel`). Rompió el crawler, el proxy y el `next build`. Los tres normalizan ese destrozo por su cuenta; aun así, `MSYS_NO_PATHCONV=1` delante.
- **`.filter(existsSync)` esconde listas muertas.** `check-tokens.js` y `contrast.js` seguían nombrando `panel.css`/`panel.js` y salían 0 igualmente, porque el filtro los tiraba. Pasaban las puertas y mentían sobre qué vigilan.


## Trampas del paso 10 (Revenue, pivote de la web, panel en claro)

- 🔴 **Una cifra escrita a mano sobrevive a todo menos a un test que la ate a su fuente.** `DEMO_COMMERCIAL_FACTS` llevaba `roomsSold: 56` y un ADR de 180 € mientras el dataset cerraba la noche con 55 habitaciones. Nadie lo vio en meses porque **nada comparaba las dos cosas**. La prueba que lo impide ahora no fija un número —eso solo mueve el problema—: comprueba que la portada y el libro de reservas dicen lo MISMO (`tests/demo/bookings.test.ts`, y en `tests/product/metricCatalog.test.ts` el `expect` de ADR se calcula, no se escribe).
- 🔴 **«Same Time Last Year» no es «last year».** Comparar el on the books de las próximas 30 noches contra el CIERRE del año pasado da −49 % siempre, y no informa de nada: un libro a medio llenar contra un año terminado. La comparación tiene que hacerse **al mismo lead time** (`asOf − 364`), y el cierre del año pasado va aparte y etiquetado. El primer calendario lo tenía bien en cinco tiles y mal justo en el de TRevPAR; lo cazó un escéptico leyendo los seis seguidos.
- 🔴 **Un gráfico de pace no puede dibujar el futuro.** La primera versión sumaba, para cada lead time, las habitaciones de las próximas 90 noches — incluidos los leads que aún no han ocurrido, es decir usando el «final» que el modelo conoce y el hotel no. Eso es un pronóstico disfrazado, y el producto no pronostica. La curva correcta fija el PERIODO objetivo y recorre los cortes de observación **hacia atrás hasta hoy**; la línea de este año termina en hoy y la única referencia de futuro es cómo acabó el año pasado, que es un hecho cerrado.
- 🔴 **La sonda de píxeles mide los GLIFOS contra sí mismos si no apagas el texto.** Midiendo la caja del título del escaparate salía «peor 1.00:1, fondo rgb(245,243,239)» — que es el propio color del texto. Hay que poner el título en `color: transparent`, capturar, y medir esa caja: entonces sale el fondo de verdad. Así se vio que el título estaba a **1,75:1** sobre la obra nueva (las capturas del panel en claro son mucho más claras que las fotos de banco que sustituyen) y se ensanchó `--sc-title-scrim` hasta 86 %/88 % con cuatro paradas: los cinco títulos pasan ahora entre 8,23:1 y 15,04:1.
- 🔴 **Un `PointerEvent` sintético sin `pointerType` no dispara el escaparate.** Los oyentes filtran por `e.pointerType === 'mouse'`, así que `new PointerEvent('pointerenter', {bubbles:true})` no hace nada y parece que el componente está roto. Hay que pasar `{bubbles:true, pointerType:'mouse'}`. Perdí una ronda dándolo por muerto.
- 🔴 **`snapshot-panel.js` NO limpia `panel/` antes de capturar.** Sin un `rm -rf panel` previo quedan los chunks `_next` del build anterior (nombres con hash distinto) y el árbol crece con 50 MB de assets muertos que además se comitean.
- **El producto NO se puede capturar desde `localhost:3000` cuando se compila con `basePath`.** La página sale en 8 KB: los Server Components se llaman a sí mismos por HTTP y con `basePath` esa ruta vive en `/panel/api/...`. Hay que capturar **a través de `_build/panel-proxy.js`** (3001 → 3000), que es exactamente para lo que existe. 119 KB en Revenue y 424 KB en Reservations es lo que tiene que salir.
- **Los servidores de la sesión anterior siguen vivos.** `next start` y el proxy quedaron escuchando en 3000 y 3001 desde las 20:05; el build nuevo no llegaba a ninguna parte y el error era solo `EADDRINUSE`. Antes de arrancar, mirar `netstat -ano | grep :3000` y las horas de arranque con `Get-Process ... StartTime`.
- **`tests/pilot/gitSafety.test.ts` falla por dónde vive el producto, no por el código.** Comprueba que sus fixtures NO están gitignoradas, y el producto entero cuelga de un repo que lo ignora (`/holdera-product-hotel-operations-v1/`, decisión del paso 9 por el desplegador de Hostinger). Es un fallo de entorno, preexistente: 384 de 385 en verde es el estado bueno.
- **`docs/METRICS.md` está GENERADO.** Tocar el catálogo de métricas y no pasar `npm run docs:metrics` deja rojo `tests/product/metricCatalogDoc.test.ts` con un mensaje que no dice que el arreglo es regenerar un documento.
- **`png-probe.js` quiere el color como ARRAY, no como hex.** `{"color": "#f5f3ef"}` revienta dentro de `luminance` con un error que no menciona el color. Es `{"color": [245,243,239]}`, y el umbral se llama `need`, no `min`.
- **`sharp` vive ahora en el producto.** `HOLDERA_SHARP_PATH` apuntaba a `…/aplomo/site/node_modules`, que ya no existe; la ruta buena es `holdera-product-hotel-operations-v1/holdera-product-hotel-operations-v1/node_modules` (Next 16 lo trae). Es lo que usa `_build/producto-art.js`.
- **Un agente que edita su propio validador merece una lectura, no una alarma automática.** El de SEO tocó `validate-meta.js` — que no era suyo — pero no relajó nada: trasladó la regla de «cinco nodos Service» a «exactamente un SoftwareApplication con cinco features» y añadió una que prohíbe que vuelva ningún Service. Lo único que había que corregir es que esa última regla vivía dentro de la rama de `index.html`, así que un Service reapareciendo en cualquier otra página pasaba entero.
- **Chrome es de todos.** Con ocho agentes verificando a la vez, `select_page` y la llamada siguiente pueden quedar separadas por la navegación de otro: navegué sin querer la pestaña de un agente a `holdera.es` a mitad de su verificación. Cada uno con su `isolatedContext`, y la verificación visual **al final**, cuando no queda nadie más dentro.

## Trampas del paso 12 (navegación blanda en un panel estático)

- 🔴 **El router de Next no lee el HTML: pide un payload.** Cualquier panel de
  Next capturado en archivos navega DURO por defecto, porque el servidor
  estático contesta con el documento donde el router espera
  `text/x-component`. El sintoma no es "va lento": es que **el estado del
  cliente se pierde** — aquí, la escena 3D montada en el layout. Para verlo:
  `curl -sL -H "RSC: 1" <url>` y mirar el `Content-Type`.
- **Next redirige las peticiones RSC sin `?_rsc=`.** Un `curl -H "RSC: 1"`
  sin `-L` devuelve 0 bytes y un 307 a la misma URL con el parámetro. El
  parámetro es solo una clave de caché: el servidor estático lo ignora y la
  regla se apoya en la CABECERA, no en la query.
- 🔴 **Los payloads hay que capturarlos POR EL PROXY**, igual que el HTML. Con
  `basePath`, los Server Components se llaman a sí mismos por `/panel/api/…`;
  contra `localhost:3000` directo el payload sale en 21 KB (página vacía) en
  vez de 31 KB. Es la misma trampa del paso 9, en su versión RSC.
- 🔴 **Un `<Link>` navega con el href de sus PROPS, no con el del DOM.** El
  HTML lleva los enlaces dos veces: el atributo que se ve y el flight data que
  hidrata. Reescribir solo el atributo deja al router yendo a la URL original.
  Por eso el conmutador de fecha enseñaba el 15 bajo `?date=2026-01-14`.
- 🔴 **En el flight data el href va SIN montar.** El router le pone el
  `basePath` al navegar, así que escribir ahí la ruta ya montada da
  `/panel/panel/date-2026-01-14/`. Dos destinos para la misma ruta:
  `routeToHref()` (montada, para el atributo del DOM) y
  `routeToLogicalHref()` (sin montar, para el payload).
- 🔴 **Las carpetas que inventa el snapshot no son rutas de la aplicación.**
  `/panel/date-2026-01-14/` no existe en el árbol de Next: si el router
  navega ahí, recibe un payload cuyo árbol es el de `/`, la URL cambia y la
  página se queda como estaba. Por eso esos clics se le quitan al router
  (interceptor en captura) y los hace el navegador. Las rutas REALES
  —habitaciones, plantas, zonas, secciones— sí navegan blando, que es donde
  está la cámara.
- 🔴 **Un iterador se consume una sola vez.** `rewrite()` recibe
  `pages.keys()`; al construir el `Set` se agota, así que pasarlo otra vez
  más abajo daba una lista vacía y el interceptor se inyectaba sin ningún
  enlace. Silencioso: el HTML salía bien, solo que sin la etiqueta.
- 🔴 **Un `width: max-content` dentro de una rejilla ENSANCHA la pista.** El
  conmutador Overview/Reservas medía 490px a un viewport de 390 y el
  `max-width: 100%` se resolvía contra esos 490. Acotado a escritorio.
- **`String.replace()` y el escape `$` + comilla invertida.** Ya estaba
  escrito para `$$`; esta vez el texto de reemplazo llevaba `` $` `` dentro de
  un comentario y me inyectó la cabecera del archivo en mitad de una función.
  Con una FUNCIÓN de reemplazo no pasa: ahí el `$` no es especial.
- **Una navegación dura mata el contexto de CDP a mitad del `evaluate`.** El
  error «Execution context was destroyed» NO es un fallo del script: es
  exactamente la señal que se está midiendo. `nav-check.js` la captura y la
  cuenta como "navegación dura".
- 🔴 **Un servidor de comparación mal montado miente a lo grande.** Para medir
  el panel viejo copié `serve.js` DENTRO de su carpeta, y `serve.js` resuelve
  su raíz como `__dirname/..`: servía la carpeta de arriba y todo eran 404.
  La medida salió «LCP 712 ms» y parecía que el rediseño lo había
  multiplicado por cinco. Bien montado: 3.121 ms el viejo, 3.886 ms el nuevo.
- 🔴🔴 **Una pestaña de `/json/new` nace EN SEGUNDO PLANO, y en segundo plano
  React no termina de hidratar.** Chrome estrangula los temporizadores de una
  página que no se ve, así que el planificador de React no llega a tener
  turno: **medido, la barra lateral tenía su fibra a los 500 ms y los 20
  enlaces de habitación seguían a CERO a los 10 segundos.** Un `<Link>` sin
  hidratar es un `<a href>` normal, el navegador lo sigue, y el verificador
  informaba de una «navegación dura» que en un navegador de verdad no existe:
  8/8 en una pestaña visible, 4/8 en la oculta, de forma reproducible. Perdí
  una hora persiguiendo un fallo del panel que era del arnés.
  **`Page.bringToFront` NO basta** si la ventana está tapada; lo que funciona
  es mantener un `Page.startScreencast` activo, que obliga al compositor a
  producir fotogramas. Lo llevan `nav-check.js` y `showcase-contrast.js`, y el
  mismo síntoma explicaba que el medidor del escaparate devolviera
  «no visible title» en las cinco pantallas.
- **Y por eso hay que esperar a la fibra DEL ENLACE que se va a pulsar**, no a
  la de uno cualquiera: la barra lateral hidrata en 250 ms y la escena del
  hotel bastante después. Esperar por `.shell-nav-item` daba por listo un
  `a.hs-room-link` que aún no lo estaba.
- **`Object.keys(el)` vale para ver la fibra de React, pero comprueba el
  elemento concreto**: sobre un nodo que aún no ha hidratado devuelve `[]`, y
  eso se confunde fácilmente con «este navegador no expone la fibra».

## Trampas del paso 11 (el panel sobre la HIG de Apple)

- 🔴 **La HIG no se puede leer con WebFetch: la web es JS y devuelve el
  título y nada más.** Detrás hay una API de documentación que sirve el
  contenido en JSON: `https://developer.apple.com/tutorials/data/design/human-interface-guidelines/<slug>.json`.
  Partiendo de la raíz y siguiendo las referencias salen **las 172 páginas**.
  El extractor está en el scratchpad de aquella sesión; si hace falta otra vez,
  son 60 líneas: leer `topicSections`, resolver `references`, y renderizar
  `primaryContentSections` (párrafos, listas, tablas y `tabNavigator`).
- 🔴 **Los alfas de etiqueta de Apple NO pasan WCAG.** `secondaryLabel` es
  `rgba(60,60,67,.60)` y mide **3,44:1** sobre blanco. Copiarlos tal cual mete
  un suspenso de accesibilidad en cada pantalla. La rampa hay que re-derivarla
  al alfa que alcanza el ratio — y contra el fondo REAL, no contra el que uno
  supone: la primera derivación midió sobre blanco y sobre el lienzo agrupado,
  y la auditoría en página encontró **146 fallos**, todos del mismo tipo: el
  texto no estaba sobre blanco, estaba sobre un FILL o un TINT puesto encima.
  Los valores buenos salen de medir sobre blanco, el lienzo agrupado,
  `--fill-3`, `--fill-4` y cada tinte de estado, y quedarse con el peor.
- 🔴 **`_build/a11y-probe.js` es la herramienta que lo caza, y mide COMPUESTO.**
  Recorre cada elemento con texto propio, sube por los ancestros acumulando
  cada capa translúcida hasta encontrar una opaca, compone el frente (que casi
  siempre lleva alfa) sobre esa pila y calcula el ratio. Se pasa con
  `node _build/shoot.js --eval-file _build/a11y-probe.js`. Probado: encontró
  146 fallos reales antes del arreglo y 0 después.
- 🔴 **En oscuro, un tinte sobre otro tinte crea una tercera superficie que
  nadie diseñó.** Una bandera «Today» dentro de una celda SELECCIONADA acabó
  sobre #785521 y el texto a 3,26:1. La regla: dentro de un bloque tintado el
  texto SUBE un escalón (a `--label-2`), no el tinte baja.
- 🔴 **Una regla sin media query, escrita 5.000 líneas más abajo, gana a la
  media query que colapsa el layout.** El bloque v5 redefinió `.app` con
  `grid-template-columns: var(--shell-width) minmax(0,1fr)` y se comió el
  `.app { grid-template-columns: 1fr }` de la línea 3563: a 390px el móvil
  medía **`260px 130px`** y toda la aplicación se iba de lado dentro de una
  columna de 130. Es la trampa del «gana la última del archivo» por el otro
  lado. Se cura restableciendo el colapso DESPUÉS, nunca moviendo el bloque v5
  arriba.
- 🔴 **Un `inline-flex` con `overflow-x: auto` no cabe: toma su ancho
  INTRÍNSECO.** Los controles segmentados de seis segmentos medían 445px
  pasara lo que pasara con el viewport, y el `overflow` no cambiaba nada
  porque la caja misma era lo ancho. Hace falta `max-width: 100%` además.
- 🔴 **Pintar un contenedor creyendo que es su línea.** Pasó dos veces:
  `.trend-guide` (717×306) llenó todo el área del gráfico de un lavado de
  tinta al 34 % —la línea es `.trend-guide-line`, de 1px— y
  `.chain-connector` convirtió cada etiqueta del linaje («computed by»,
  «from») en una barra gris de lado a lado. Antes de dar un `background` a
  algo que suena a «regla», mira su caja.
- 🔴 **La cadena de Evidence YA dibuja su espina**, como pareja:
  `.chain-link::before` es el raíl gris y `::after` es el haz verde que baja
  trazándolo, los dos anclados en `--rail-x`. Redefinir `::before` deja el haz
  bajando por la nada.
- **`revenue.css` y `bookings.css` cargan DESPUÉS de `globals.css`**, así que
  a igualdad de especificidad ganan ellas. Todo lo que la capa global cambie
  para esas dos superficies hay que volver a decirlo allí — por eso cada una
  tiene su propia sección v5. Lo mismo con `.metric-row-name .metric-short`
  (0,2,0) frente a un `.metric-short` suelto.
- **`tests/app/spatial.test.tsx` lee el ÚLTIMO bloque de `prefers-reduced-motion`
  del archivo** y exige que apague la cámara, los trazos de la escena y las
  view transitions. Es el instinto correcto —el último bloque es el que gana—
  así que el bloque final de v5 lo restablece todo y es el que hay que editar
  a partir de ahora.
- **Un `<dl>` solo admite dt/dd (o un `<div>` que los envuelva).** Los
  `<p class="bkd-note">` dentro de `.bkd-figures` y el `<p class="chain-help">`
  dentro de `.ev-kv` rompían el árbol de accesibilidad: Lighthouse bajaba
  `definition-list` y `agent-accessibility-tree` a 0 (accesibilidad 97,
  agentic 50 en Reservas). La nota es una SEGUNDA descripción del mismo
  término, o sea un segundo `<dd>` — y entonces hay que decirle que no herede
  los 22px de `.bkd-figures dd`.
- **El escaparate del hero se mide con el título en `color: transparent`.**
  `node _build/showcase-contrast.js` lo hace entero: dispara el hover con
  `pointerType: 'mouse'` (sin eso los oyentes lo ignoran), pone el título
  transparente, captura y mide los píxeles de la caja de los GLIFOS. Con la
  obra nueva: peor 9,35:1 contra un mínimo de 3:1.
- **`_build/shoot.js` y `_build/lcp-bench.js` hablan CDP directamente contra
  el Chrome de trabajo (:9222) y abren su PROPIA pestaña.** Verificar un
  rediseño son decenas de pantallas por dos viewports y dos esquemas; pedirlas
  una a una por el MCP es lentísimo y, con otra sesión viva, la pestaña
  seleccionada puede cambiar a mitad.
- 🔴 **Un servidor de comparación mal montado miente a lo grande.** Para medir
  el panel viejo contra el nuevo copié `serve.js` DENTRO de la carpeta del
  panel viejo — y `serve.js` resuelve su raíz como `__dirname/..`, así que
  servía la carpeta de arriba y todo eran 404. La medida salió «LCP 712 ms» y
  parecía que el rediseño lo había multiplicado por cinco. Montado bien: 3.121
  ms el viejo, 3.886 ms el nuevo, sin comprimir y con la CPU a 4×; son +76 KB
  de CSS en crudo, que con brotli son ~8 KB. **Una comprobación que no cuadra
  no se explica, se investiga.**
- **El bloque v4 NO se puede borrar aunque sus 87 tokens estén todos
  redeclarados por v5**: dentro vive la isla de tokens de `.hs`, que es lo que
  mantiene la escena del hotel sobre el fondo para el que se pintaron sus 42
  materiales.
- 🔴 **Las imágenes del escaparate se sirven `immutable` UN AÑO, y son
  capturas del panel.** El `.htaccess` da a los `.webp`
  `Cache-Control: public, max-age=31536000, immutable`: el navegador ni
  siquiera revalida. Los veinte archivos de `assets/img/producto/` cambian en
  cada rediseño del panel, así que con la URL igual se quedan rancios un año
  en cada máquina que ya haya visitado holdera.es — la trampa del paso 8, y
  peor, porque el css caduca a los siete días y esto no caduca. Desde el paso
  11 `node _build/version-assets.js` **también los sella**, leyendo `src=` y
  `data-bg=` (el escaparate los pide en diferido por ese atributo), acotado a
  `assets/img/producto/`. El resto de imágenes del sitio son estables y su
  `immutable` es justo lo que se quiere.

## Trampas de este proyecto (cuestan tiempo si se olvidan)
- **Nunca uses `String.replace()` con `$$` en el texto de reemplazo**: `$$` es un escape y se convierte en `$`. Rompió tres veces los helpers `$$(...)` de `script.js` (los selectores de lista quedaron como `$(...)` y reventó todo el JS). Usa `split(a).join(b)`.
- Los scripts de parcheo van en un **archivo** (`Write` + `node fichero.js`), no en `node -e` **ni en un here-doc de bash**: las dos formas se comen `$` y las comillas. Un here-doc con `<<'JS'` reventó igual (`unexpected EOF`); usa la herramienta de escritura de archivos.
- La máscara de degradado del robot en móvil **ya no existe**: el cerebro flota y no se corta contra el borde inferior, así que no había nada que tapar.
- **`preserveDrawingBuffer`**: `toDataURL()` y `readPixels()` sobre el canvas devuelven vacío porque WebGL limpia el buffer al presentar el frame. `_build/brain-poster.html` reclama el contexto con `preserveDrawingBuffer: true` en un `<script>` **antes** del `brain.js` diferido; la segunda llamada a `getContext` devuelve ese mismo contexto e ignora sus atributos, así que producción no paga el flag.
- **Un canvas redimensionado se queda en blanco si nadie lo repinta.** Con `prefers-reduced-motion` el bucle se para tras un frame: el manejador de `resize` tiene que repintar cuando no hay bucle (`if (!raf && ready) drawOnce(...)`), no cuando la sección está fuera de pantalla.
- `simple-icons` **no cubre la lista con una sola versión**: v12+ quitó OpenAI y Microsoft Azure, y n8n solo existe a partir de v14. `_build/logos/` instala las dos con alias de npm (`si-current`, `si-legacy`) y el generador coge cada marca de la que la tenga.
- El ancho mínimo de una ventana de Chrome en Windows es ~485 px: para verificar de verdad a 390 px hay que **emular el dispositivo**, no redimensionar la ventana.
- 🔴 **Un `fov` vertical fijo NO encoge un lienzo estrecho: lo RECORTA por los lados.** Con `perspective(FOV, W/H)` el cerebro perdía hasta un 44 % de los puntos en una tablet en vertical (1024×1366 cae del lado de escritorio del corte de 900 px). `brain.js` abre el `fov` en vertical por debajo de `FIT_ASPECT` para comportarse como `object-fit: contain`. A 1440×900 no se recortaba nada: **este fallo no se ve en la captura de referencia**.
- 🔴 **`getBoundingClientRect()` mide DESPUÉS de las transformaciones CSS.** `resize()` se llama en cada frame y la entrada del hero escala `.hero__figure` de 0.97 a 1 durante 1100 ms: medir con el rect reasignaba el buffer de dibujo ~30 veces por carga. Usa `offsetWidth`/`offsetHeight`, que son la caja de layout.
- **La nube de puntos va DEBAJO del copy (`z-index: 1`).** El robot podía ir encima porque era una silueta opaca, estrecha y anclada abajo; una nube difusa de 494 px de ancho salpica el titular. A 1440 px no hay ninguna posición horizontal que libere a la vez el titular y el aside (la nube es 494 px y el hueco 377 px): la capa es la única solución.
- **Con `prefers-reduced-motion` la pose no puede depender del scroll.** Solo se pinta un frame, y puede repintarse en cualquier posición: si la pose viene del scroll, ese repintado dibuja el cerebro hundido y con `alpha` 0, es decir, un cuadrado vacío que ya no se repinta nunca.
- **Un realce blanco sobre una superficie casi blanca no existe.** Los biseles `inset 0 1px 1px` heredados de la hoja oscura medían 1.00:1 sobre `#ffffff`. Se quitaron en vez de inventar un bisel nuevo. Si vuelves a necesitarlo: en claro el bisel es una sombra abajo, no un brillo arriba.
- **Un campo de formulario blanco sobre un panel casi blanco es 1.04:1**: el borde es lo ÚNICO que identifica el control y WCAG 1.4.11 pide 3:1. Por eso existe `--sh-field-border` (0.50) aparte de `--sh-border-strong` (0.20), que debe seguir siendo un pelo fino en los `.tag`.
- Las casillas nativas **ignoran `border-color` y `box-shadow`**: `outline: none` en `.form input:focus` dejaba la casilla de consentimiento sin ningún foco visible. La regla excluye `[type="checkbox"]` a propósito.
- 🔴 **Un verificador con los valores escritos a mano miente en las dos direcciones.** La primera versión de `contrast.js` fijaba `--accent` para `.steps__num`; cuando el CSS pasó a `--accent-ink`, siguió dando FALLO por un problema ya arreglado — y encima **salía con código 0**, así que nunca habría parado nada. Un verificador que da un falso positivo se deja de leer, y entonces tampoco ve el fallo de verdad. Regla: **el verificador lee el archivo real, y antes de fiarte de él rómpelo a propósito** y comprueba que efectivamente falla (`_build/contrast.js` está probado contra tres regresiones: color, borde y regla renombrada).

## Pendiente / TODO con Alex
- 🔴 **Defecto de accesibilidad PREEXISTENTE, sin tocar (decisión de Alex).** `.btn--on-dark.btn--secondary` usa `--on-ink-border-strong` (blanco al 16 %), que compone a **1,59:1** sobre `--panel` y sobre `--fab-bg`. Ese contorno es lo ÚNICO que identifica el botón de cerrar del drawer y el de MENÚ de la isla flotante, y WCAG 1.4.11 pide 3:1. Es el mismo fallo que ya se corrigió en la hoja con `--sh-control-border` (0,36) y que se acaba de corregir en la nav del escaparate. **Arreglo de una línea**: cambiar `--on-ink-border-strong` por `--sh-control-border` en `.btn--on-dark.btn--secondary`. No aplicado porque cambia el aspecto de dos controles que Alex ya dio por buenos.
- Confirmar idioma (ES solo, o ES/EN).
- 🔴 **El fuente del producto no entra en el commit, y es a propósito.** `holdera-product-hotel-operations-v1/` está en `.gitignore` desde el paso 9 porque Hostinger manda el repo entero al desplegador de Node en cuanto encuentra un `package.json`. Los cambios del paso 10 están en esa carpeta (exportable tal cual) y llegan a la web a través de `panel/`, que sí se comitea. Si Alex quiere el fuente versionado, lo suyo es **un repositorio aparte**: no se puede meter aquí sin romper el despliegue. Decisión suya.
- 🔴 **Los supuestos de F&B, spa y otros servicios son inventados (declarados, pero inventados).** `DEMO_ANCILLARY_MODEL` en `bookings.ts` fija 1,72 huéspedes por habitación, 68 % de desayunos a 14,50 €, ticket de spa de 82 €, etc. Es lo que hace que TRevPAR exista y la pantalla lo dice ("modelled from declared assumptions"), pero **son números de demo**: si Alex tiene cifras reales de un hotel piloto, se cambian ahí y toda la superficie comercial se mueve con ellas.
- **El crecimiento interanual de la demo también está declarado**: `DEMO_YOY_RATE_GROWTH` 5,5 % y `DEMO_YOY_PACE_SHIFT` 4,5 %. Son lo que hace que la comparación con STLY tenga algo que contar; sin ellos las dos líneas se superponen. Confirmar que la historia que cuentan (el hotel va mejor que el año pasado) es la que Alex quiere enseñar en una demo comercial.
- **Capturas del escaparate**: `assets/img/producto/*.webp` son capturas del panel en su estado de hoy. Cualquier rediseño del panel las deja desfasadas; se rehacen con cinco capturas de página entera en `assets/img/producto/_raw/<clave>-full.png` y `node _build/producto-art.js`.
- 🔴 **Datos de contacto**: email, teléfono, WhatsApp e Instagram (y LinkedIn) son marcadores `[… por confirmar]` en el pie de las siete páginas y en `contacto.html`; sustituir texto Y `href` (lista: `node _build/check-placeholders.js`). Después, añadir `contactPoint`/`sameAs` a `shared.organization` en `_build/seo/meta.json` y reinyectar. Backend del formulario (`#contacto` y `contacto.html`) sin conectar.
- **Dominio: RESUELTO el 09-09-2026.** Es `https://holdera.es` y está en vivo. Pendiente de la lista antigua: **Search Console + enviar el sitemap**, y **Google Business Profile** (`_build/seo/CHECKLIST.md`).
- 🔴 **El formulario de contacto sigue sin backend.** Hoy valida y dice que no está conectado. En cuanto Alex dé un email, se conecta y se quita el aviso. Es lo que más cuesta cada día que pasa: la web está publicada y captando visitas sin ninguna vía de contacto operativa.
- 🔴 **La historia pública de GitHub conserva las rutas locales.** Los commits `f6d06ec` y `8f36e1f` incluyen `_build/seo/build-og.js` con la ruta a `…/aplomo/site/node_modules`, o sea el nombre de otro cliente. El código actual ya está saneado, pero la historia no. Reescribirla (son pocos commits) o dejarlo: decisión de Alex.
- Legal (RGPD/LSSI): `aviso-legal.html` y `privacidad.html` existen con marcadores `[Razón social]`, `[NIF]`, `[Domicilio social]`, `[Email de contacto]`, `[fecha]`; rellenarlos. El formulario ya lleva casilla de consentimiento y capa informativa básica y enlaza a privacidad desde el pie.
- Reseñas reales de clientes antes de añadir la sección de testimonios.
